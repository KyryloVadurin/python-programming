# Перелік тестових питань

## Тема 1

### РОЗДІЛ 1. АРХІТЕКТУРА CPYTHON, КОНВЕЄР КОМПІЛЯЦІЇ ТА PVM

**1. У якій послідовності здійснюється обробка вихідного коду програми (.py) в еталонному інтерпретаторі CPython до моменту виконання?**  
а) Лексичний аналіз (токени) → Синтаксичний аналіз (AST) → Компіляція у байт-код (PyCodeObject) → Виконання у PVM  
б) Синтаксичний аналіз (AST) → Лексичний аналіз → Генерація машинного коду → Виконання ОС  
в) Пряма трансляція у машинний код центрального процесора → Компіляція → Виконання у PVM  
г) Лексичний аналіз → Компіляція у C-код → Асемблювання → Лінкування  

**2. Яке головне функціональне призначення файлів із розширенням .pyc, що розміщуються у каталозі `__pycache__`?**  
а) Кешування скомпільованого байт-коду для прискорення повторного завантаження модулів  
б) Захист інтелектуальної власності шляхом шифрування від декомпіляції  
в) Збереження резервних копій коду для відновлення після збоїв  
г) Збереження профілю пам'яті для оптимізації роботи збирача сміття  

**3. Які компоненти входять до службового заголовка кешованого файлу байт-коду .pyc у CPython? *(Оберіть усі правильні варіанти)***  
а) Чотирибайтне магічне число версії байт-коду  
б) Бітове поле прапорців для визначення стратегії перевірки цілісності кешу  
в) Метадані відповідності вихідного коду (часова мітка модифікації або хеш)  
г) Вихідний текст коментарів та документаційних рядків docstrings  
д) Таблиця символів сторонніх системних бібліотек C++  

**4. Який архітектурний тип обчислювача покладено в основу віртуальної машини Python (PVM)?**  
а) Абстрактний стековий процесор без фізичних регістрів загального призначення  
б) Регістровий процесор із фіксованим набором апаратних регістрів  
в) Акумуляторна обчислювальна машина з одним регістром даних  
г) Тензорний процесор із конвеєрною матричною пам'яттю  

**5. Яку функцію виконує файл `ceval.c` у вихідному коді ядра CPython під час роботи програми?**  
а) Містить головний цикл інтерпретації, що по черзі вибирає та виконує інструкції байт-коду  
б) Виконує синтаксичний аналіз коду та будує синтаксичне дерево AST  
в) Здійснює зв'язування з бібліотеками операційної системи на етапі запуску  
г) Відповідає за виділення фізичних сторінок пам'яті комп'ютера  

**6. У стандартному інтерпретаторі CPython механізм РОЗДІЛування GIL дозволяє кільком нативним потокам одного процесу паралельно виконувати інструкції байт-коду на різних ядрах процесора.**  
а) Істина  
б) Хибність  

**7. Яка першочергова причина впровадження глобального РОЗДІЛування інтерпретатора (GIL) в архітектурі CPython?**  
а) Захист структур пам'яті від стану гонитви через неатомарне оновлення лічильників посилань  
б) Прискорення операцій введення-виведення у мережевих з'єднаннях  
в) Обмеження максимального споживання оперативної пам'яті одним процесом  
г) Забезпечення сумісності байт-коду з іншими мовами програмування  

**8. За допомогою якого макросу низькорівневі бібліотеки обчислень (NumPy, OpenCV) можуть тимчасово звільнити м'ютекс GIL для паралельної обробки даних на всіх ядрах ЦП?**  
а) `Py_BEGIN_ALLOW_THREADS`  
б) `Py_RELEASE_GIL_LOCK`  
в) `Py_THREAD_PARALLEL_START`  
г) `Py_UNBLOCK_INTERPRETER_MUTEX`  

**9. Лексичний аналізатор інтерпретатора CPython перетворює синтаксичне дерево AST безпосередньо у машинний двійковий код.**  
а) Істина  
б) Хибність  

**10. Встановіть відповідність між компонентом конвеєра виконання CPython та його функціональним призначенням:**  
*Компоненти:*  
1. Лексичний сканер  
2. Синтаксичний парсер  
3. Компілятор CPython  
4. Віртуальна машина PVM  

*Призначення:*  
А) Побудова ієрархічного абстрактного синтаксичного дерева AST  
Б) Трансляція синтаксичного дерева у байт-код об'єкта `PyCodeObject`  
В) Розбиття вхідного потоку символів на неподільні лексеми (токени)  
Г) Покрокове виконання інструкцій байт-коду на динамічному стеку  

**11. Яка внутрішня системна структура CPython створюється динамічно для кожного виклику функції з метою ізоляції контексту виконання, збереження локальних змінних та посилань на стек?**  
а) `PyFrameObject`  
б) `PyCodeObject`  
в) `PyTypeObject`  
г) `PyMethodObject`  

**12. Як називається віртуальна машина інтерпретатора Python, що виконує команди байт-коду? Введіть загальноприйняту трилітерну абревіатуру англійськими літерами:**  
*(Впишіть відповідь)*: _______________

---

### РОЗДІЛ 2. ІНФРАСТРУКТУРА РОЗРОБНИКА, ВЕРСІОНУВАННЯ ТА ІНСТРУМЕНТИ

**13. У чому полягає принципова архітектурна відмінність між менеджером Miniconda (conda) та вбудованим модулем venv?**  
а) Conda ізолює бінарні залежності рівня ОС, версії C-бібліотек та інтерпретатора, тоді як venv ізолює виключно Python-пакети  
б) Venv компілює вихідний код у машинний код, а Conda працює лише з байт-кодом  
в) Venv створює віртуальні машини апаратного рівня, а Conda є виключно редактором коду  
г) Між ними немає функціональної відмінності, це повністю ідентичні інструменти  

**14. Які каталоги та файли обов'язково вносять до файлу .gitignore інженерного проекту Python для запобігання збереженню артефактів? *(Оберіть усі правильні варіанти)***  
а) `__pycache__/`  
б) `*.pyc`  
в) `.venv/`  
г) Головний файл коду програми `main.py`  
д) Файл фіксації залежностей `requirements.txt`  

**15. Вбудований у стандартну бібліотеку модуль venv здатен самостійно завантажувати та інсталювати драйвери апаратних прискорювачів обчислень CUDA без участі хостової ОС.**  
а) Істина  
б) Хибність  

**16. Встановіть відповідність між інструментом інженерної екосистеми та його ключовою роллю:**  
*Інструменти:*  
1. Git  
2. Visual Studio Code  
3. Jupyter Notebook  
4. Miniconda  

*Ролі:*  
А) Системне керування ізольованими середовищами та бінарними C-залежностями  
Б) Розподілений контроль версій на базі спрямованого ациклічного графа комітів  
В) Інтегроване середовище розробки з підтримкою протоколу LSP  
Г) Інтерактивне середовище для досліджень та візуалізації на базі протоколу ZeroMQ  

**17. За яким стандартизованим протоколом мовний сервер Pylance взаємодіє з редактором Visual Studio Code для забезпечення аналізу коду та автодоповнення?**  
а) Language Server Protocol (LSP)  
б) Remote Procedure Call (RPC)  
в) Simple Object Access Protocol (SOAP)  
г) ZeroMQ Messaging Protocol  

**18. Яка математична структура даних лежить в основі збереження історії станів та комітів у розподіленій системі Git?**  
а) Спрямований ациклічний граф (DAG)  
б) Двійкове дерево пошуку (BST)  
в) Кільцевий буфер фіксованої довжини  
г) Лінійний однозв'язний список  

---

### РОЗДІЛ 3. МОДЕЛЬ ПАМ'ЯТІ CPYTHON, СИСТЕМА ТИПІЗАЦІЇ ТА ПОСИЛАННЯ

**19. Яка комбінація характеристик точно описує систему типізації мови програмування Python?**  
а) Динамічна та сильна (строга)  
б) Статична та сильна (строга)  
в) Динамічна та слабка  
г) Статична та слабка  

**20. Чому виконання виразу `1 + "2"` у мові Python викликає аварійне завершення з винятком TypeError замість отримання `3` або `"12"`?**  
а) Через сильну (строгу) типізацію, яка забороняє автоматичне неявне приведення несумісних типів  
б) Через статичну типізацію змінних, що фіксується компілятором  
в) Через дію РОЗДІЛування GIL у процесі обчислень  
г) Через переповнення стека віртуальної машини PVM  

**21. Які два обов'язкові службові поля містить заголовок базової структури PyObject для кожного об'єкта в динамічній пам'яті CPython? *(Оберіть дві правильні відповіді)***  
а) `ob_refcnt` (лічильник активних посилань на об'єкт)  
б) `ob_type` (покажчик на структуру опису типу даних)  
в) `ob_name` (текстове ім'я змінної в просторі імен)  
г) `ob_hash` (обов'язковий попередньо обчислений криптографічний хеш)  

**22. Що повертає вбудована функція `id(obj)` для довільного об'єкта в еталонній реалізації інтерпретатора CPython?**  
а) Фізичну адресу структури PyObject у віртуальному адресному просторі процесу  
б) Порядковий номер виділення пам'яті з моменту запуску інтерпретатора  
в) Кількість активних змінних, що вказують на цей об'єкт  
г) Розмір виділеної під об'єкт пам'яті у байтах  

**23. У чому полягає фундаментальна різниця між оператором `is` та оператором `==` у мові Python?**  
а) Оператор `is` перевіряє тотожність адрес у пам'яті, а `==` перевіряє еквівалентність значень об'єктів  
б) Оператор `is` порівнює числові значення, а `==` порівнює типи даних  
в) Оператор `is` завжди виконує неявне приведення типів перед порівнянням  
г) Між ними немає жодної різниці, вони є повними синтаксичними синонімами  

**24. Усі базові скалярні типи даних у мові Python (int, float, bool, complex) є строго незмінними (immutable) об'єктами.**  
а) Істина  
б) Хибність  

**25. Для якого числового діапазону цілих чисел CPython створює статичний глобальний пул об'єктів-сінглтонів (Small Integer Cache) під час запуску середовища?**  
а) Від -5 до 256 включно  
б) Від 0 до 255 включно  
в) Від -128 до 127 включно  
г) Від -1024 до 1024 включно  

**26. Які результати повернуть вирази `(a is b)` та `(u is v)`, якщо в окремих рядках стандартної консолі CPython виконати: `a = 256; b = 256; u = 257; v = 257`?**  
а) `a is b` поверне True, а `u is v` поверне False  
б) Обидва вирази повернуть True  
в) Обидва вирази повернуть False  
г) `a is b` поверне False, а `u is v` поверне True  

**27. Як називається 64-бітне цілочисельне поле заголовка структури PyObject, що відстежує поточну кількість посилань на об'єкт?**  
*(Впишіть відповідь)*: _______________

**28. Якщо лічильник посилань `ob_refcnt` незмінного скалярного об'єкта падає до нуля, CPython негайно звільняє пам'ять без участі циклічного збирача сміття.**  
а) Істина  
б) Хибність  

---

### РОЗДІЛ 4. БАЗОВІ СКАЛЯРНІ ТИПИ: INT, FLOAT, COMPLEX, BOOL, NONETYPE

**29. Як мова Python 3 запобігає переповненню розрядної сітки процесора при роботі з надвеликими цілими числами для типу int?**  
а) Застосовує довгу арифметику довільної точності на базі структури `PyLongObject`  
б) Автоматично перетворює число у тип float з плаваючою комою  
в) Усікає старші розряди до фіксованої ширини 64 біти  
г) Викликає системне апаратне переривання OverflowError  

**30. Скільки бітів пам'яті виділено безпосередньо під збережену дробову частину мантиси у стандарті IEEE 754 для типу float у CPython?**  
а) 52 біти  
б) 64 біти  
в) 32 біти  
г) 11 бітів  

**31. Чому результат порівняння `(0.1 + 0.2 == 0.3)` у мові Python дорівнює False?**  
а) Числа 0.1 та 0.2 у двійковій системі перетворюються на нескінченні періодичні дроби і зазнають похибки заокруглення  
б) Оператор `==` принципово не підтримує порівняння дійсних чисел типу float  
в) Тип float автоматично округлює всі результати до двох знаків після коми  
г) Число 0.3 під час виконання неявно інтерпретується як комплексне число  

**32. З яких компонентів складається внутрішнє представлення комплексного числа (тип complex) у CPython на рівні структури PyComplexObject?**  
а) З двох 64-бітних полів типу double мови C (дійсна та уявна частини)  
б) З одного 128-бітного цілого числа у додатковому коді  
в) З покажчика на текстовий рядок вихідного коду вигляду `a+bj`  
г) З масиву 32-бітних чисел фіксованої коми  

**33. У мові Python логічний тип даних bool є прямим підкласом типу цілих чисел int, тому вираз `issubclass(bool, int)` повертає True.**  
а) Істина  
б) Хибність  

**34. Чому виклик `sys.getsizeof(0)` на 64-розрядній системі CPython повертає 24 або 28 байтів, тоді як примітивний int у мові C займає лише 4 байти?**  
а) Через обов'язковий системний оверхед структури PyObject (лічильник посилань, покажчик типу та довжина масиву цифр)  
б) Тому що інтерпретатор автоматично дублює число для підтримки багатопоточності  
в) Через автоматичне збереження повного імені змінної всередині самого числа  
г) Через обов'язкове резервування пам'яті під механізм циклічного збирача сміття  

**35. Встановіть відповідність між скалярним типом мови Python та його системним представленням у CPython:**  
*Типи даних:*  
1. int  
2. float  
3. complex  
4. NoneType  

*Представлення:*  
А) 64-бітне число з плаваючою комою подвійної точності стандарту IEEE 754  
Б) Єдиний статичний системний об'єкт-сінглтон `_PyNoneStruct`  
В) Структура `PyLongObject` довгої арифметики за основою 2 у степені 30  
Г) Структура `PyComplexObject` із двома 64-бітними полями типу double  

**36. Який метод об'єкта комплексного числа z у Python повертає комплексно спряжене число (із протилежним знаком уявної частини)?**  
а) `z.conjugate()`  
б) `z.inverse()`  
в) `z.symmetric()`  
г) `z.conj_negate()`  

**37. Назвіть спеціальний вбудований об'єкт-сінглтон мови Python, який використовується для позначення відсутності результату або порожнього стану:**  
*(Впишіть відповідь)*: _______________

**38. Дійсну та уявну частини комплексного числа `z = 3.0 + 4.5j` можна отримати безпосередньо через атрибути `z.real` та `z.imag` у вигляді чисел типу float.**  
а) Істина  
б) Хибність  

---

### РОЗДІЛ 5. ІСТИННІСТЬ ОБ'ЄКТІВ (TRUTHINESS) ТА КОРОТКА СХЕМА ОБЧИСЛЕННЯ

**39. Які з наведених значень при оцінці у логічному контексті Python вважаються хибними (False)? *(Оберіть усі правильні варіанти)***  
а) `None`  
б) `0.0`  
в) Порожній список `[]`  
г) Порожній рядок `""`  
д) Рядок `"False"`  
е) Список із одним нулем `[0]`  

**40. У якому порядку інтерпретатор CPython перевіряє методи об'єкта для визначення його логічної істинності (truthiness)?**  
а) Спочатку викликає `__bool__()`; якщо його немає — викликає `__len__()`; якщо обидва відсутні — об'єкт вважається True  
б) Спочатку викликає `__len__()`; якщо довжина відсутня — безумовно повертає False  
в) Викликає виключно метод `__eq__()` для порівняння з числом 0  
г) Перетворює об'єкт на текстовий рядок і перевіряє наявність символу "0"  

**41. Яким буде фактичний результат обчислення виразу: `[] or "fallback_node"`?**  
а) `"fallback_node"`  
б) `True`  
в) `False`  
г) `[]`  

**42. Яким буде фактичний результат обчислення виразу: `0 and "packet_received"`?**  
а) `0`  
б) `False`  
в) `"packet_received"`  
г) `None`  

**43. Чому умовна конструкція `(channel is not None and channel.is_active())` є безпечною, навіть якщо змінна `channel` дорівнює None?**  
а) Завдяки короткій схемі оператор and бачить хибність першого операнда і не викликає правий метод  
б) Тому що метод `is_active()` автоматично повертає False при виклику від None  
в) Тому що оператор is автоматично перехоплює всі винятки у виразі  
г) Тому що віртуальна машина PVM ігнорує винятки у РОЗДІЛах умовних операторів  

**44. Логічні оператори and та or у Python завжди повертають виключно значення типу bool (строго True або False).**  
а) Істина  
б) Хибність  

---

### РОЗДІЛ 6. КЕРУВАННЯ ПОТОКОМ ІНСТРУКЦІЙ ТА СИНТАКСИС ВІДСТУПІВ

**45. Які спеціальні псевдолексеми генерує лексичний сканер CPython для сигналізації парсеру про відкриття та закриття вкладеного лексичного РОЗДІЛу коду?**  
а) `INDENT` та `DEDENT`  
б) `OPEN_BLOCK` та `CLOSE_BLOCK`  
в) `BEGIN_SCOPE` та `END_SCOPE`  
г) `TAB_IN` та `TAB_OUT`  

**46. Яку вимогу до просторового оформлення РОЗДІЛів коду встановлює промисловий стандарт PEP 8?**  
а) Рівно 4 пробіли на кожен рівень вкладеності; використання символів табуляції категорично заборонено  
б) 1 знак табуляції на кожен рівень вкладеності  
в) 2 пробіли або 1 табуляція за індивідуальним вибором розробника  
г) Будь-яка кількість пробілів за умови, що вона є парним числом  

**47. Яку ключову властивість має тримісний умовний вираз вигляду `(x = val_a if condition else val_b)` у мові Python?**  
а) Є виразом (expression), завжди повертає конкретне значення та обчислює лише одну відповідну гілку  
б) Є інструкцією (statement), тому не може використовуватися всередині аргументів виклику функцій  
в) Завжди обчислює обидві гілки (`val_a` та `val_b`) перед перевіркою умови  
г) Автоматично приводить кінцевий результат вибору до типу bool  

**48. Якщо у повній умовній конструкції if-elif-else кілька альтернативних умов elif є істинними, PVM послідовно виконає кодові РОЗДІЛи всіх істинних гілок.**  
а) Істина  
б) Хибність  

---

### РОЗДІЛ 7. ПОБІТОВІ ОПЕРАЦІЇ ТА АПАРАТНЕ МАСКУВАННЯ

**49. Яким буде цілочисельний результат виконання побітової інверсії `~9` у мові Python відповідно до формули додаткового коду `~x = -(x + 1)`?**  
а) -10  
б) -9  
в) 8  
г) -8  

**50. У виразі `(reg & 0x0010 == 0x0010)` при `reg = 0x0010` результат дорівнює False. У чому полягає причина такої поведінки?**  
а) Оператор рівності `==` має вищий пріоритет виконання, ніж побітова кон'юнкція `&`  
б) Стається арифметичне переповнення 16-бітної розрядної сітки  
в) Шістнадцяткові літерали не підтримують побітове порівняння без функції `int()`  
г) Умовний оператор не підтримує числа як результат порозрядного множення  

**51. Який побітовий вираз використовується для читання та нормалізації значення k-го біта (результат 0 або 1) у цілочисельному регістрі S?**  
а) `(S & (1 << k)) >> k`  
б) `(S | (1 << k)) >> k`  
в) `(S ^ (1 << k)) << k`  
г) `S >> (k & 1)`  

**52. Який вираз слід застосувати для примусового скидання в 0 значення k-го біта у змінній S зі збереженням інших розрядів?**  
а) `S & ~(1 << k)`  
б) `S | (1 << k)`  
в) `S ^ (1 << k)`  
г) `S & (1 << k)`  

**53. Вкажіть символ мови Python, що реалізує операцію побітового виключного АБО (XOR) над цілими числами:**  
*(Впишіть символ)*: _____

**54. Побітовий зсув невід'ємного цілого числа ліворуч `(x << n)` у мові Python математично строго еквівалентний операції множення x на 2 у степені n.**  
а) Істина  
б) Хибність  

---

### РОЗДІЛ 8. ОБМЕЖЕННЯ ТОЧНОСТІ IEEE 754 ТА ФУНКЦІЯ MATH.ISCLOSE

**55. Чому під час порівняння величин, що прагнуть до нуля, у функції `math.isclose(a, b, rel_tol=1e-09, abs_tol=0.0)` необхідно явно задавати `abs_tol`?**  
а) Тому що відносний допуск `rel_tol` при множенні на близьке до нуля число дає нульовий поріг похибки  
б) Тому що параметр `rel_tol` за замовчуванням викликає помилку ділення на нуль ZeroDivisionError  
в) Тому що числа, близькі до нуля, автоматично конвертуються у ціле число 0  
г) Тому що стандарт IEEE 754 забороняє відносні допуски для від'ємних чисел  

**56. Встановіть відповідність між математичним інструментом мови Python та його специфічним призначенням:**  
*Інструменти:*  
1. `math.isclose`  
2. `decimal.Decimal`  
3. `cmath`  
4. `sys.float_info.epsilon`  

*Призначення:*  
А) Прецизійна десяткова арифметика з фіксованою точністю без двійкових похибок  
Б) Визначення машинного нуля (мінімальної розрізнюваної різниці мантиси)  
В) Порівняння дійсних чисел float з урахуванням відносного та абсолютного допусків  
Г) Обчислення аналітичних та трансцендентних функцій для комплексних чисел  

**57. Який небажаний ефект у системі стандарту IEEE 754 називають «катастрофічною втратою значущих цифр»?**  
а) Втрату розрядів точності мантиси під час операції віднімання двох дуже близьких за значенням великих дійсних чисел  
б) Переповнення зміщеної експоненти дійсного числа з перетворенням результату на нескінченність  
в) Автоматичне округлення результату ділення дійсних чисел до найближчого цілого числа  
г) Помилку виділення оперативної пам'яті у стековому кадрі віртуальної машини PVM  

---

### РОЗДІЛ 9. ПОТОКОВЕ ВВЕДЕННЯ-ВИВЕДЕННЯ ТА ФОРМАТУВАННЯ ДАНИХ

**58. Який зі стандартних системних потоків дескрипторів модуля sys за замовчуванням є небуферизованим для негайного виведення діагностичних помилок?**  
а) `sys.stderr`  
б) `sys.stdout`  
в) `sys.stdin`  
г) `sys.stdlog`  

**59. Чому форматування рядків за допомогою f-рядків (`f"..."`) демонструє вищу швидкість виконання, ніж метод `str.format()` або оператор `%`?**  
а) f-рядки оптимізуються компілятором і транслюються безпосередньо у низькорівневу інструкцію байт-коду `BUILD_STRING`  
б) f-рядки вимикають перевірку типів під час форматування  
в) f-рядки виконуються окремим апаратним процесорним ядром через GIL  
г) f-рядки автоматично записують результат безпосередньо у відеопам'ять термінала  

**60. Які специфікатори мікросинтаксису форматування f-рядків відповідають за наведені інженерні операції над числовими даними? *(Оберіть усі правильні варіанти)***  
а) Символ `'b'` у секції типу представляє ціле число у двійковій системі числення  
б) Символ `'x'` або `'X'` здійснює форматування числа у шістнадцятковий код  
в) Запис `'.2f'` фіксує виведення рівно двох знаків після десяткової коми для числа float  
г) Символ підкреслення `'_'` використовується для візуального групування тисячних розрядів  
д) Символ `'c'` автоматично виконує піднесення числа до квадрата  
е) Символ `'i'` перетворює дійсне число на комплексний об'єкт

## Тема 2

### РОЗДІЛ 1. СИНТАКСИС РОЗГАЛУЖЕНЬ, PEP 8 ТА ТОКЕНІЗАЦІЯ ВІДСТУПІВ

**1.** Що генерує лексичний аналізатор інтерпретатора CPython у разі повернення коду на попередній (менший) рівень відступу?  
а) Один або декілька службових токенів DEDENT  
б) Службовий токен INDENT  
в) Спеціальний виняток IndentationReset  
г) Символ кінця файлу EOF  

**2.** З якої мови програмування творець Python Гвідо ван Россум запозичив концепцію синтаксичного виділення РОЗДІЛів за допомогою відступів (*off-side rule*)?  
а) ABC  
б) Algol 68  
в) Pascal  
г) C  

**3.** За якої умови під час лексичного аналізу коду в середовищі Python генерується виняток `IndentationError`?  
а) Поточний рівень відступу рядка не збігається із жодним зі значень у системному стеку відступів  
б) Рівень вкладеності РОЗДІЛів перевищує 10 рівнів  
в) Користувач використав парну кількість пробілів замість непарної  
г) Після інструкції `if` використано більше 8 пробілів  

**4.** Яку вимогу до оформлення рівнів вкладеності коду встановлює міжнародний стандарт PEP 8?  
а) Рівно 4 пробіли на кожен рівень вкладеності; використання символів табуляції суворо заборонено  
б) 1 символ табуляції (Tab) на кожен рівень вкладеності  
в) Довільна кількість пробілів від 2 до 8 за вибором розробника  
г) 2 пробіли для умовних операторів і 4 пробіли для циклічних конструкцій  

**5.** У мові Python 3 дозволяється вільно змішувати символи табуляції та пробіли в межах одного файлу для формування відступів одного РОЗДІЛу коду.  
а) Правильно (True)  
б) Неправильно (False)  

**6.** Яка низькорівнева інструкція байт-коду CPython зазвичай використовується для переходу за мітку у разі хибності предиката в умовному операторі `if`?  
а) POP_JUMP_IF_FALSE  
б) JUMP_ABSOLUTE_IF_TRUE  
в) GOTO_BRANCH_FALSE  
г) SWITCH_CASE_EVAL  

**7.** За яким принципом віртуальна машина Python виконує аналіз умов у каскадній конструкції `if-elif-else`?  
а) Строго зверху вниз до першого істинного предиката, після чого інші гілки ігноруються  
б) Одночасно обчислює всі предикати і виконує РОЗДІЛ із найвищим пріоритетом  
в) Знизу вгору, починаючи з термінального РОЗДІЛу `else`  
г) У довільному порядку, залежно від оптимізатора компілятора  

**8.** Які твердження щодо синтаксичної організації РОЗДІЛів коду в Python є правильними? *(Оберіть усі правильні варіанти)*  
а) Рівень відступу визначає просторові межі РОЗДІЛу інструкцій  
б) Зменшення рівня відступу генерує один або декілька токенів DEDENT  
в) Символ двокрапки обов'язково передує початку вкладеного РОЗДІЛу інструкцій  
г) Стандарт PEP 8 дозволяє вільно чергувати табуляцію і пробіли  
д) Оператор `if` завжди вимагає обов'язкової наявності РОЗДІЛів `elif` та `else`  

**9.** Якщо у каскаді `if-elif-else` одночасно кілька умов у гілках `elif` виявляються істинними, інтерпретатор виконає кодові РОЗДІЛи кожної з цих істинних гілок.  
а) Правильно (True)  
б) Неправильно (False)  

**10.** Який виняток згенерує інтерпретатор Python 3, якщо у вихідному коді програми виявлено несумісне змішування символів табуляції та пробілів у відступах?  
*(Дайте коротку письмову відповідь)*  

---

### РОЗДІЛ 2. КОНЦЕПЦІЯ ІСТИННОСТІ ОБ'ЄКТІВ (TRUTHINESS) ТА C-СЛОТИ

**11.** Як називається внутрішня властивість об'єктів у мові Python, що дозволяє оцінювати їх як логічні предикати без явного приведення до типу `bool`?  
а) Концепція істинності об'єктів (*truthiness*)  
б) Концепція качиної типізації (*duck typing*)  
в) Динамічне зв'язування (*dynamic binding*)  
г) Абсолютна еквівалентність (*identity equivalence*)  

**12.** Який спеціальний метод або внутрішній слот мови C інтерпретатор CPython опитує в першу чергу під час визначення істинності об'єкта функцією `PyObject_IsTrue`?  
а) `tp_as_number->nb_bool` (метод `__bool__`)  
б) `tp_as_sequence->sq_length` (метод `__len__`)  
в) `tp_as_mapping->mp_subscript` (метод `__getitem__`)  
г) `tp_compare` (метод `__eq__`)  

**13.** Що перевіряє інтерпретатор CPython для контейнерного об'єкта у логічному контексті, якщо метод `__bool__` у класі об'єкта не реалізовано?  
а) Метод `__len__` (слот `sq_length`): нульова довжина трактується як `False`, а додатна — як `True`  
б) Автоматично генерує виняток `TypeError` через відсутність булевого слота  
в) Викликає метод `__hash__` для порівняння хеш-коду з нулем  
г) Завжди прирівнює такий об'єкт до значення `False`  

**14.** Яким буде булевий еквівалент створеного користувацького об'єкта, якщо в його класі не визначено ані методу `__bool__`, ані методу `__len__`?  
а) Завжди `True`  
б) Завжди `False`  
в) Залежить від обсягу займаної об'єктом оперативної пам'яті  
г) Викликає виняток `NotImplementedError`  

**15.** Які з наведених об'єктів при оцінці у логічному контексті Python вважаються хибними (`False`)? *(Оберіть усі правильні варіанти)*  
а) Дійсне число `0.0`  
б) Порожній рядок `""`  
в) Спеціальний об'єкт `None`  
г) Порожній кортеж `()`  
д) Текстовий рядок `"False"`  
е) Список із одним нульовим елементом `[0]`  

**16.** Від якого базового типу даних мови Python успадковано клас булевих значень `bool`?  
а) `int`  
б) `float`  
в) `object`  
г) `NoneType`  

**17.** Чи повертає вираз `issubclass(bool, int)` у мові Python значення `True`?  
а) Так (True)  
б) Ні (False)  

**18.** Чому конструкція `if status is True:` вважається некоректною порівняно з `if status:`?  
а) Вона перевіряє фізичну ідентичність об'єкта в пам'яті із сінглтоном `True`, ігноруючи поліморфну істинність інших об'єктів  
б) Вона викликає критичну помилку часу виконання у версіях Python 3.10 і вище  
в) Вона змушує віртуальну машину виділяти новий стековий кадр  
г) Вона не працює, якщо змінна `status` має логічний тип `bool`  

**19.** Встановіть відповідність між типом даних у CPython та системним критерієм визначення його хибності:  
**Тип даних:**  
1) `str`  
2) `dict`  
3) `NoneType`  
4) `set`  

**Критерій визначення хибності:**  
А) Збіг адреси пам'яті покажчика з глобальним сінглтоном `Py_None`  
Б) Перевірка рівності нулю поля довжини рядка `ob_size`  
В) Перевірка рівності нулю лічильника заповненості таблиці (`used == 0`)  
Г) Перевірка відсутності записів у таблиці хешів (`ma_used == 0`)  

**20.** Який булевий результат поверне явне приведення викликом `bool(range(0))`?  
а) `False`  
б) `True`  
в) `None`  
г) Виникне виняток `ValueError`  

---

### РОЗДІЛ 3. ЛОГІЧНІ ОПЕРАТОРИ ТА КОРОТКА СХЕМА ОБЧИСЛЕННЯ

**21.** Що повертають бінарні логічні оператори `and` та `or` у мові Python?  
а) Один із об'єктів-операндів, на якому завершився обчислювальний процес  
б) Завжди виключно значення канонічного булевого типу (`True` або `False`)  
в) Ціле число `1` або `0`  
г) Кортеж, що містить обидва операнди  

**22.** Який тип даних завжди повертає унарний логічний оператор `not` у мові Python?  
а) `bool`  
б) `int`  
в) Тип вхідного операнда  
г) `NoneType`  

**23.** У якому порядку (від найвищого пріоритету до найнижчого) виконуються операції у виразах без дужок у Python?  
а) Операції порівняння → `not` → `and` → `or`  
б) `not` → `and` → `or` → операції порівняння  
в) `and` → `or` → `not` → операції порівняння  
г) Операції порівняння → `and` → `or` → `not`  

**24.** Яким є математичне правило обчислення виразу `x and y` за короткою схемою (*short-circuit evaluation*)?  
а) Якщо `bool(x)` дорівнює `False`, обчислення припиняється і повертається `x`; інакше обчислюється і повертається `y`  
б) Завжди обчислюються обидва операнди `x` та `y`, після чого повертається більше значення  
в) Якщо `bool(x)` дорівнює `True`, повертається `x` без обчислення `y`  
г) Якщо `bool(y)` дорівнює `True`, повертається `x`; інакше повертається `y`  

**25.** Яким є математичне правило обчислення виразу `x or y` за короткою схемою?  
а) Якщо `bool(x)` дорівнює `True`, обчислення припиняється і повертається `x`; інакше обчислюється і повертається `y`  
б) Якщо `bool(x)` дорівнює `False`, повертається `x` без обчислення `y`  
в) Якщо `bool(y)` дорівнює `True`, повертається `y`; інакше повертається `x`  
г) Обидва операнди обчислюються завжди незалежно від значення `x`  

**26.** Яким буде результат виконання виразу `0 or "" or [42] or "ERROR"`?  
а) `[42]`  
б) `0`  
в) `""`  
г) `"ERROR"`  

**27.** Яким буде кінцевий результат виконання виразу `(None or 256) and "STATUS"`?  
а) `"STATUS"`  
б) `256`  
в) `None`  
г) `True`  

**28.** Яке головне призначення інженерного патерну захисних виразів (*guard clauses*) на базі короткої схеми обчислення?  
а) Запобігання виклику аварійних операцій (ділення на нуль, вихід за межі масиву) за допомогою попередньої перевірки умов  
б) Прискорення компіляції вихідного коду в машинний код  
в) Зменшення обсягу споживання пам'яті стеком викликів  
г) Автоматичне приведення типів операндів до типу `float`  

**29.** У виразі `(len(items) > 0) and (items[0] == 10)` виникне помилка `IndexError`, якщо список `items` є порожнім.  
а) Правильно (True)  
б) Неправильно (False)  

**30.** Яким буде вміст списку `result` після виконання наступного коду?
```python
flags = [0, 12, 0, 45]
result = [f for f in flags if f and 100 // f > 2]
```
а) `[12]`  
б) `[12, 45]`  
в) `[0, 12, 45]`  
г) Виникне виняток `ZeroDivisionError`  

---

### РОЗДІЛ 4. ТЕРНАРНИЙ УМОВНИЙ ВИРАЗ ТА СЕЛЕКЦІЯ ДАНИХ

**31.** Яку синтаксичну структуру має тернарний умовний вираз у мові Python згідно зі специфікацією PEP 308?  
а) `E_true if Condition else E_false`  
б) `Condition ? E_true : E_false`  
в) `if Condition then E_true else E_false`  
г) `E_true ? Condition ! E_false`  

**32.** У чому полягає принципова відмінність між класичною інструкцією `if-else` та тернарним умовним виразом?  
а) Тернарний оператор є виразом (*expression*), що редукується до об'єкта і повертає значення, а `if-else` є інструкцією (*statement*)  
б) Тернарний оператор не підтримує логічні оператори `and` та `or`  
в) Інструкція `if-else` виконується швидше на рівні процесорних команд  
г) Тернарний вираз завжди приводить кінцевий результат до типу `bool`  

**33.** У чому полягає лінивий характер обчислення тернарного виразу `x = f() if cond else g()`?  
а) Спочатку обчислюється умова `cond`, після чого викликається лише одна відповідна функція: `f()` або `g()`  
б) Функції `f()` та `g()` обчислюються паралельно у різних потоках  
в) Обидві функції `f()` та `g()` обчислюються заздалегідь до перевірки умови `cond`  
г) Жодна з функцій не викликається, доки змінна `x` не буде використана у наступному рядку  

**34.** Яку математичну операцію над сигналом реалізує наступна інженерна конструкція?
```python
u_out = U_MAX if u_in > U_MAX else u_in
```
а) Клемпування (лінійне обмеження максимальної амплітуди сигналу)  
б) Швидке перетворення Фур'є для гармонік сигналу  
в) Інверсію фази вхідного сигналу  
г) Диференціювання функції напруги за часом  

**35.** У мові Python синтаксично дозволено створювати вкладені ланцюжки тернарних виразів виду `a if c1 else b if c2 else d`.  
а) Правильно (True)  
б) Неправильно (False)  

---

### РОЗДІЛ 5. ЦИКЛ WHILE, ЗБІЖНІСТЬ ТА АРИФМЕТИКА ДІЙСНИХ ЧИСЕЛ

**36.** За якої умови виконується кожна чергова ітерація циклу з передумовою `while`?  
а) Доки значення контрольного логічного виразу умови оцінюється як `True`  
б) Фіксовану кількість разів, передану в аргументі циклу  
в) Доки лічильник ітерацій менший за розмір системного стека  
г) Тільки якщо попередня ітерація завершилася викликом `break`  

**37.** Що називають «варіантом циклу» у формальному доведенні коректності та завершуваності ітераційного алгоритму?  
а) Цілочисельну функцію стану, що строго монотонно спадає на кожному кроці та обмежена знизу нулем  
б) Умову, яка залишається незмінною протягом усього часу виконання циклу  
в) Змінну, що приймає випадкові значення під час кожної ітерації  
г) Список винятків, які можуть виникнути в тілі циклу  

**38.** Що розуміють під поняттям «інваріант циклу»?  
а) Логічне твердження щодо співвідношення змінних, яке є істинним до початку циклу та зберігається після кожної ітерації  
б) Константу, яка визначає максимальну кількість кроків циклу  
в) Системний реєстр процесора, що відповідає за переривання  
г) Булевий прапорець, що змінює значення на протилежне на кожному кроці  

**39.** Чому використання умови `while val != 1.0:` при інкременті `val += 0.1` є грубою методологічною помилкою?  
а) Через похибки двійкового представлення чисел IEEE 754 накопичене значення ніколи точно не зрівняється з 1.0, спричиняючи нескінченний цикл  
б) Тому що тип `float` не підтримує операцію нерівності `!=`  
в) Тому що оператор `+=` заборонено застосовувати до змінних типу `float`  
г) Тому що інтерпретатор автоматично перетворить число `1.0` на ціле `1`  

**40.** Яка конструкція є коректною для організації умови виходу в циклі чисельної апроксимації для дійсних чисел із точністю `eps`?  
а) `while abs(val - target) > eps:`  
б) `while val != target:`  
в) `while val == target:`  
г) `while val is not target:`  

**41.** Скільки ітерацій виконає наведений цикл у середовищі CPython до моменту зупинки?
```python
val = 0.0
while val != 0.3:
    val += 0.1
    if val > 0.5:
        break
```
а) 6 ітерацій  
б) 3 ітерації  
в) 4 ітерації  
г) Цикл виконуватиметься нескінченно  

**42.** Цикл `while` у мові Python є циклом із постумовою, тобто його тіло гарантовано виконується щонайменше один раз до перевірки виразу.  
а) Правильно (True)  
б) Неправильно (False)  

**43.** Які фактори можуть призвести до перетворення циклу `while` на нескінченний цикл (зациклення)? *(Оберіть усі правильні варіанти)*  
а) Відсутність модифікації стану змінних умови всередині тіла циклу  
б) Перевірка строгої рівності дійсних чисел типу `float` у предикаті виходу  
в) Предикатний вираз умови є тотожно істинним (`True`) для всіх можливих станів  
г) Використання відступів у 4 пробіли за стандартом PEP 8  
д) Виклик інструкції `break` за настання граничної умови  

---

### РОЗДІЛ 6. ЦИКЛ FOR, ПРОТОКОЛ ІТЕРАТОРА ТА МУТАЦІЯ КОЛЕКЦІЙ

**44.** Який фундаментальний патерн об'єктно-орієнтованого проектування покладено в основу реалізації циклу `for` у мові Python?  
а) Ітератор (*Iterator*)  
б) Фабричний метод (*Factory Method*)  
в) Одинак (*Singleton*)  
г) Спостерігач (*Observer*)  

**45.** Який магічний метод обов'язково повинен реалізувати об'єкт, щоб вважатися ітерованим (*iterable*) у сучасному протоколі Python?  
а) `__iter__`  
б) `__next__`  
в) `__len__`  
г) `__getitem_value__`  

**46.** Який метод викликає віртуальна машина PVM на кожному кроці проходження ітератора для послідовного отримання чергового елемента?  
а) `__next__`  
б) `__iter__`  
в) `__step__`  
г) `__pop__`  

**47.** Який службовий виняток сигналізує віртуальній машині CPython про вичерпання елементів ітератора та штатне завершення циклу `for`?  
а) `StopIteration`  
б) `IndexError`  
в) `GeneratorExit`  
г) `EndOfIterationError`  

**48.** Виняток `StopIteration` під час виконання циклу `for` сприймається як фатальний збій програми і вимагає обов'язкового ручного перехоплення через `try-except`.  
а) Правильно (True)  
б) Неправильно (False)  

**49.** До якого наслідку призводить видалення або додавання елементів у список (`list`) безпосередньо в тілі циклу `for`, що ітерується за цим же списком?  
а) Десинхронізація внутрішнього покажчика ітератора, що призводить до пропуску або дублювання обробки елементів  
б) Миттєвий виклик аварійного винятку `ModificationError`  
в) Автоматичне створення незмінної резервної копії списку  
г) РОЗДІЛування процесора м'ютексом GIL  

**50.** Що станеться під час виконання коду, якщо розмір словника (`dict`) змінюється (додано або видалено ключ) під час ітерації по ньому у циклі `for`?  
а) Виникне виняток `RuntimeError: dictionary changed size during iteration`  
б) Цикл успішно завершиться, проігнорувавши нові елементи  
в) Словник буде автоматично перетворено на кортеж  
г) Інтерпретатор перейде у нескінченний цикл  

**51.** Встановіть відповідність між сутністю протоколу ітерування та її системною роллю:  
**Сутність протоколу:**  
1) Ітерований об'єкт (*Iterable*)  
2) Екземпляр ітератора (*Iterator*)  
3) Виняток `StopIteration`  
4) Функція `iter()`  

**Системна роль:**  
А) Вбудований інструмент, що викликає метод `__iter__` для отримання ітератора  
Б) Службовий протокольний сигнал про повне вичерпання елементів послідовності  
В) Об'єкт зі станом, що реалізує метод `__next__` для видачі наступного значення  
Г) Контейнер, який реалізує метод `__iter__` та повертає об'єкт ітератора  

**52.** Будь-який стандартний ітератор у Python є ітерованим об'єктом (*iterable*), оскільки його метод `__iter__` повертає сам об'єкт `self`.  
а) Правильно (True)  
б) Неправильно (False)  

---

### РОЗДІЛ 7. ГЕНЕРАТОР ПОСЛІДОВНОСТЕЙ RANGE ТА СКЛАДНІСТЬ O(1)

**53.** Якою є просторова складність (обсяг займаної пам'яті) об'єкта `range(0, 10**12)` у мові Python 3?  
а) O(1) — константна просторова складність  
б) O(N) — лінійна залежність від кількості чисел  
в) O(log N) — логарифмічна складність  
г) O(N^2) — квадратична складність  

**54.** Скільки байтів пам'яті фіксовано займає внутрішня структура `PyRangeObject` на 64-розрядній платформі CPython незалежно від кількості чисел у діапазоні?  
а) 48 байтів  
б) 8 байтів  
в) 1024 байти  
г) Залежить від різниці між `stop` та `start`  

**55.** Чому операція перевірки входження числа `x in range(start, stop, step)` виконується миттєво за час O(1)?  
а) Вона обчислюється аналітично через перевірку меж та остачі від ділення без ітераційного перебору  
б) Інтерпретатор попередньо завантажує весь масив чисел у кеш другого рівня (L2 CPU cache)  
в) Діапазон зберігається як відсортоване бінарне дерево пошуку  
г) Операція делегується векторним регістрам SIMD  

**56.** Яким буде числове значення `len(r)` для діапазону `r = range(10, 0, 2)`?  
а) 0  
б) 5  
в) 4  
г) Виникне виняток `ValueError`  

---

### РОЗДІЛ 8. ДИРЕКТИВИ BREAK, CONTINUE, PASS ТА РОЗДІЛ ELSE У ЦИКЛАХ

**57.** Яку дію виконує інструкція `break` у тілі циклічної конструкції?  
а) Примусово достроково завершує виконання циклу та передає керування на інструкцію за його межами  
б) Пропускає решту поточної ітерації та переходить до наступного кроку циклу  
в) Зупиняє роботу всієї програми з викликом системного винятку  
г) Скидає лічильник циклу до початкового значення  

**58.** Яку дію здійснює інструкція `continue` під час виконання ітерації циклу?  
а) Негайно припиняє виконання поточної ітерації та передає керування на початок наступного кроку  
б) Здійснює повний вихід із циклу з переходом у РОЗДІЛ `else`  
в) Тимчасово призупиняє потік виконання на задану кількість мілісекунд  
г) Видаляє поточний елемент із вихідної колекції даних  

**59.** За якої умови виконується РОЗДІЛ `else`, асоційований із циклічною конструкцією `for` або `while`?  
а) Виключно тоді, коли цикл завершився природним штатно вичерпаним шляхом без виклику оператора `break`  
б) Тільки тоді, коли цикл було достроково перервано інструкцією `break`  
в) Щоразу після кожної окремої ітерації циклу  
г) Тільки у разі виникнення необробленого винятку всередині циклу  

**60.** Який результат буде виведено на екран у результаті виконання фрагмента коду?
```python
for item in []:
    break
else:
    print("EMPTY_PROCESSED")
```
а) EMPTY_PROCESSED  
б) Жодного повідомлення виведено не буде  
в) Виникне помилка `IndexError`  
г) Виникне помилка `UnboundLocalError`

## Тема 3

### РОЗДІЛ 1. ВНУТРІШНЯ СТРУКТУРА ТА ОРГАНІЗАЦІЯ ПАМ'ЯТІ

**1. Що безпосередньо зберігає внутрішній неперервний масив `ob_item` структури `PyListObject` у CPython?**
* а) Масив 64-розрядних покажчиків на структури `PyObject` у купі
* б) Сирі бінарні значення елементів у послідовному двійковому форматі
* в) Хеш-таблицю ключів та зміщень елементів у віртуальній пам'яті
* г) Двозв'язний список адрес вузлів із метаданими типів

**2. За якою формулою обчислюється фізична адреса покажчика на елемент `L[i]` списку в 64-розрядній архітектурі CPython?**
* а) `BaseAddress(ob_item) + i * 8 байтів`
* б) `BaseAddress(ob_item) + i * sizeof(PyListObject)`
* в) `BaseAddress(ob_item) + (i * ob_size) / allocated`
* г) `BaseAddress(ob_item) + i * 4 байти`

**3. Якою є асимптотична часова складність операції довільного читання елемента за індексом `L[i]` у списку Python?**
* а) $O(1)$
* б) $O(n)$
* в) $O(\log n)$
* г) $O(n^2)$

**4. У середовищі CPython елементи списку (наприклад, числа або рядки) розташовуються безпосередньо один за одним у неперервному апаратному блоці пам'яті.**
* а) Правильно
* б) Неправильно

**5. Які поля обов'язково входять до складу низькорівневої C-структури `PyListObject` у CPython? *(Оберіть усі правильні відповіді)***
* а) `ob_item` (покажчик на масив покажчиків `PyObject**`)
* б) `allocated` (виділена фізична місткість буфера в слотах)
* в) `ob_size` (поточна логічна кількість збережених елементів)
* г) `free_list` (системний масив бінарних дерев пошуку)
* д) `hash_value` (попередньо обчислений хеш-код списку)

**6. Як називається принцип організації пам'яті списків Python, за якого масив містить лише покажчики на об'єкти, розпорошені у різних ділянках купи?**
* а) Непряма адресація (indirect addressing)
* б) Асоціативна адресація
* в) Пряма регістрова адресація
* г) Кеш-локальна адресація

**7. Який фізичний розмір має один покажчик на об'єкт `PyObject*` у масиві списку на 64-розрядній платформі?**
* а) 4 байти
* б) 8 байтів
* в) 16 байтів
* г) 56 байтів

**8. Час вилучення посилання на елемент списку `L[i]` за індексом залежить від загальної кількості елементів $n$ у списку.**
* а) Правильно
* б) Неправильно

**9. Встановіть відповідність між атрибутом структури `PyListObject` та його системною роллю:**

*Атрибути:*
1. `ob_item`
2. `allocated`
3. `ob_size`
4. `ob_refcnt`

*Описи:*
* А) Поточна виділена ємність системного буфера в слотах
* Б) Неперервний масив покажчиків на елементи списку
* В) Лічильник активних посилань на об'єкт списку
* Г) Фактична кількість елементів у послідовності

**10. Вкажіть асимптотичну часову складність довільного доступу до елемента списку за його індексом у нотації О-велике.**  
*(Впишіть коротку відповідь)*

---

### РОЗДІЛ 2. ІНДЕКСАЦІЯ, ЗРІЗИ ТА ПОВЕРХНЕВІ ЕФЕКТИ

**11. За якою формулою інтерпретатор CPython нормалізує від'ємний індекс $i$ для послідовності довжиною $n$?**
* а) $n + i$
* б) $n - i$
* в) $|i|$
* г) $n - |i| - 1$

**12. Який виняток генерує середовище виконання Python при спробі звернення за індексом, що виходить за межі діапазону $[0, n - 1]$ після нормалізації?**
* а) `IndexError`
* б) `ValueError`
* в) `KeyError`
* г) `OverflowError`

**13. Яким буде значення елемента `matrix[1][0]` після виконання операцій:**
```python
row = [0, 0]
matrix = [row] * 3
matrix[0][0] = 99
```
* а) 0
* б) 99
* в) `None`
* г) Виникне помилка `IndexError`

**14. Чому виконання повного зрізу `T[:]` для кортежу повертає посилання на той самий об'єкт (`id(T) == id(T[:])`), а для списку `L[:]` створює новий об'єкт?**
* а) Кортеж незмінний (immutable), тому створення копії зайве; список змінюваний і вимагає нового контейнера
* б) Кортеж завжди автоматично кешується в таблиці глобальних імен модуля
* в) Зріз кортежу повертає генератор, а зріз списку — масив
* г) Операція зрізу над кортежем заборонена специфікацією і викликає попередження

**15. Яку кількість елементів міститиме результуючий зріз послідовності `L[1:10:2]`?**
* а) 4
* б) 5
* в) 9
* г) 10

**16. Які твердження щодо механізму зрізів у мові Python є правильними? *(Оберіть усі правильні відповіді)***
* а) Зріз списку створює новий об'єкт `PyListObject` із поверхневою копією покажчиків
* б) Зрізи не викликають виняток `IndexError` при виході значень `start` або `stop` за межі списку
* в) При від'ємному кроці `step` напрямок сканування масиву змінюється на протилежний
* г) Зріз списку рекурсивно клонує всі вкладені мутабельні структури
* д) Вираз `L[::-1]` повертає генераторний ітератор замість нового списку

**17. Операція зрізу списку `L[:]` створює повну глибоку копію (deep copy) всіх вкладених об'єктів.**
* а) Правильно
* б) Неправильно

**18. Які системні значення меж `start` та `stop` за замовчуванням встановлює Python для зрізу списку довжиною $n$ при від'ємному кроці (`step < 0`)?**
* а) `start = n - 1, stop = -(n + 1)`
* б) `start = 0, stop = n`
* в) `start = n, stop = 0`
* г) `start = -1, stop = 0`

**19. Яке число буде виведено в результаті виконання коду:**
```python
nums = [10, 20, 30, 40, 50]
print(nums[-2])
```
* а) 20
* б) 30
* в) 40
* г) 50

**20. Вкажіть точну назву стандартного винятку, що виникає при спробі звернення до неіснуючого індексу списку.**  
*(Впишіть коротку відповідь)*

---

### РОЗДІЛ 3. КОРТЕЖІ, ОПТИМІЗАЦІЇ ТА ХЕШОВАНІСТЬ

**21. У чому полягає принципова відмінність структури `PyTupleObject` від `PyListObject` на рівні мови C?**
* а) Кортеж не має поля `allocated` і виділяє пам'ять точно під розмір `ob_size` через struct hack
* б) Кортеж зберігає об'єкти у вигляді неперервного двійкового дерева
* в) Кортеж зберігає копії даних безпосередньо у коді інструкцій байт-коду
* г) Кортеж використовує 32-розрядні покажчики замість 64-розрядних

**22. Який розмір у байтах мають порожній список та порожній кортеж відповідно на 64-розрядній платформі CPython?**
* а) 40 байтів та 56 байтів
* б) 56 байтів та 40 байтів
* в) 64 байти та 32 байти
* г) 0 байтів та 0 байтів

**23. Для кортежів якої максимальної довжини CPython використовує внутрішній пул оптимізації пам'яті (`free_list`) після їх знищення?**
* а) Тільки для порожнього кортежу (0 елементів)
* б) До 20 елементів включно
* в) До 256 елементів
* г) До 512 елементів

**24. У CPython усі порожні кортежі є посиланнями на один і той самий глобальний об'єкт-сінглтон (`id(()) == id(tuple())`).**
* а) Правильно
* б) Неправильно

**25. За якої умови об'єкт типу `tuple` є хешованим (hashable) і може виступати ключем словника?**
* а) Будь-який кортеж завжди хешований завдяки своїй незмінності
* б) Якщо кожен без винятку елемент кортежу є рекурсивно хешованим
* в) Якщо довжина кортежу не перевищує 20 елементів
* г) Якщо кортеж містить хоча б одне числове значення

**26. Який виняток виникне при спробі виконати вираз `hash((1, 2, [3, 4]))`?**
* а) `TypeError: unhashable type: 'list'`
* б) `ValueError: mutable container inside tuple`
* в) `KeyError: invalid hashable key`
* г) `AttributeError: 'list' object has no attribute 'hash'`

**27. Які з наведених об'єктів можуть успішно використовуватись як ключі словника `dict` у Python? *(Оберіть усі правильні відповіді)***
* а) `(1, 2, "sensor_A")`
* б) `frozenset([1, 2, 3])`
* в) `(10, (20, 30))`
* г) `[1, 2, 3]`
* д) `(1, [2, 3])`

**28. У класі `list` атрибут `__hash__` явно встановлено у значення `None`, що сигналізує інтерпретатору про заборону хешування.**
* а) Правильно
* б) Неправильно

**29. Встановіть відповідність між типом послідовності та його властивостями змінюваності і хешованості:**

*Типи послідовностей:*
1. `list`
2. `tuple`
3. `str`
4. `bytes`

*Властивості:*
* А) Незмінний (immutable), хешований (за умови хешованості елементів)
* Б) Змінюваний (mutable), нехешований
* В) Незмінний (immutable) сирий буфер октетів, завжди хешований
* Г) Незмінний (immutable), завжди хешований

**30. Чому мережевий дескриптор 5-tuple (IP джерела/призначення, порти, протокол) інженерно доцільно зберігати у вигляді `tuple`, а не `list`?**
* а) Забезпечує захист від мутації, менше споживання RAM і можливість бути ключем у словнику сесій
* б) Кортеж дозволяє динамічно додавати поля маршрутизації під час руху пакета
* в) Кортеж автоматично шифрує мережеві байти за алгоритмом CRC32
* г) Швидкість обходу списку у циклі `for` вища, ніж кортежу

---

### РОЗДІЛ 4. ДИНАМІЧНЕ МАСШТАБУВАННЯ ТА СКЛАДНІСТЬ ОПЕРАЦІЙ

**31. Яку стратегію застосовує CPython у функції `list_resize()` для запобігання квадратичній вартості наповнення списку?**
* а) Геометричне надлишкове виділення пам'яті з коефіцієнтом розширення близько 1.125
* б) Виділення пам'яті строго на 1 слот більше при кожному виклику `append`
* в) Подвоєння виділеної пам'яті (множення на 2) при кожному розширенні
* г) Фіксоване збільшення буфера рівно на 1024 слоти незалежно від розміру

**32. Який приблизний відсоток надлишкової пам'яті резервує алгоритм масштабування списку CPython для великих масивів?**
* а) Менше 1%
* б) Близько 12.5% (коефіцієнт зростання 9/8)
* в) Рівно 50%
* г) Рівно 100%

**33. Якою є амортизована часова складність додавання одного елемента в кінець списку через метод `append()`?**
* а) $O(1)$
* б) $O(n)$
* в) $O(\log n)$
* г) $O(n^2)$

**34. Чому операція `list.insert(0, value)` має лінійну часову складність $O(n)$?**
* а) Необхідність повного рекурсивного перерахунку хеш-таблиці списку
* б) Вимога фізичного зсуву всіх існуючих $n$ покажчиків у пам'яті праворуч за допомогою `memmove()`
* в) Автоматичне перетворення списку на кортеж перед вставкою
* г) Повільний пошук нульового індексу за допомогою лінійного сканування

**35. Якою буде загальна асимптотична складність обробки черги з $n$ пакетів при регулярному використанні `list.pop(0)`?**
* а) $O(1)$
* б) $O(n)$
* в) $O(n \log n)$
* г) $O(n^2)$

**36. Які з наведених операцій над списком `list` мають асимптотичну складність $O(1)$ у найгіршому або амортизованому випадку? *(Оберіть усі правильні відповіді)***
* а) `L.append(x)`
* б) `L.pop()` (вилучення з кінця)
* в) `len(L)`
* г) `L.insert(0, x)`
* д) `L.pop(0)`

**37. Метод `list.pop()` без аргументів вилучає останній елемент списку за константний час $O(1)$, оскільки не вимагає зсуву сусідніх покажчиків у пам'яті.**
* а) Правильно
* б) Неправильно

**38. Яка системна C-функція викликається в ядрі CPython для РОЗДІЛового зміщення покажчиків усередині масиву `ob_item` при операціях `insert` та `pop(0)`?**
* а) `malloc`
* б) `memmove`
* в) `strcpy`
* г) `bzero`

**39. Вкажіть часову складність операції додавання елемента на початок списку довжиною $n$ (метод `insert(0, x)`) у нотації О-велике.**  
*(Впишіть коротку відповідь)*

**40. Кожен поодинокий виклик методу `list.append()` обов'язково призводить до системного виклику `realloc()` операційної системи.**
* а) Правильно
* б) Неправильно

---

### РОЗДІЛ 5. АЛГОРИТМ ТА ХАРАКТЕРИСТИКИ TIMSORT

**41. Який гібридний алгоритм сортування використовується за замовчуванням у стандартних реалізаціях `list.sort()` та `sorted()` у Python?**
* а) Quicksort
* б) Heapsort
* в) Timsort
* г) Radix Sort

**42. Поєднанням яких двох класичних алгоритмів сортування є Timsort?**
* а) Швидке сортування (Quicksort) та сортування бульбашкою (Bubble Sort)
* б) Сортування злиттям (Merge Sort) та сортування бінарними вставками (Binary Insertion Sort)
* в) Пірамідальне сортування (Heapsort) та сортування вибором (Selection Sort)
* г) Порозрядне сортування (Radix Sort) та сортування підрахунком (Counting Sort)

**43. Які з перелічених властивостей притаманні алгоритму Timsort? *(Оберіть усі правильні відповіді)***
* а) Стабільність (збереження відносного порядку однакових ключів)
* б) Адаптивність (швидкість $O(n)$ на частково впорядкованих масивах)
* в) Часова складність $O(n \log n)$ у найгіршому випадку
* г) Константна просторова складність $O(1)$ без виділення допоміжних буферів
* д) Нестабільний порядок однакових елементів після завершення

**44. У яких межах алгоритм Timsort зазвичай обирає параметр `minrun` для балансування глибини злиття?**
* а) Від 2 до 8
* б) Від 32 до 64
* в) Від 128 до 256
* г) Рівно 1024

**45. У чому полягає ключова різниця між викликами `list.sort()` та `sorted(list)`?**
* а) `list.sort()` модифікує список на місці і повертає `None`; `sorted()` повертає новий відсортований список
* б) `sorted()` працює швидше, оскільки використовує багатопотокові інструкції процесора
* в) `list.sort()` приймає лише кортежі, а `sorted()` — виключно масиви чисел
* г) `list.sort()` використовує Quicksort, тоді як `sorted()` застосовує Timsort

**46. Що означає математична властивість стабільності (stability) алгоритму сортування?**
* а) Гарантія неперевищення часу роботи алгоритму понад лінійний ліміт
* б) Здатність сортувати елементи без використання додаткової пам'яті
* в) Збереження початкового відносного порядку розташування елементів із еквівалентними ключами
* г) Неможливість генерації винятків під час порівняння об'єктів

**47. Коли алгоритм Timsort перемикається у спеціальний «режим галопу» (Galloping Mode)?**
* а) Коли вхідний масив перевищує обсяг оперативної пам'яті 1 ГБ
* б) Коли один із РОЗДІЛів даних перемагає в операціях порівняння понад 7 разів поспіль
* в) Коли виявляється відсутність монотонності у перших 32 елементах
* г) Коли всі елементи масиву є екземплярами рядкового типу

**48. У найкращому випадку (на вже впорядкованому або зворотно впорядкованому масиві) алгоритм Timsort виконується за лінійний час $O(n)$.**
* а) Правильно
* б) Неправильно

**49. Яким буде порядок елементів після виконання коду:**
```python
sorted([(10, 0.5), (5, 0.2), (10, 0.1), (5, 0.8)], key=lambda x: x[0])
```
* а) `[(5, 0.2), (5, 0.8), (10, 0.5), (10, 0.1)]`
* б) `[(5, 0.8), (5, 0.2), (10, 0.1), (10, 0.5)]`
* в) `[(10, 0.5), (10, 0.1), (5, 0.2), (5, 0.8)]`
* г) `[(10, 0.1), (10, 0.5), (5, 0.2), (5, 0.8)]`

**50. Встановіть відповідність між поняттям алгоритму Timsort та його описом:**

*Поняття:*
1. `minrun`
2. `Galloping Mode`
3. Стабільність
4. Сортування на місці

*Описи:*
* А) Експоненційний бінарний пошук меж включення при незбалансованому злитті
* Б) Мінімальний розмір впорядкованого підмасиву для злиття
* В) Модифікація внутрішнього буфера покажчиків без виділення другого списку
* Г) Збереження взаємного порядку записів із однаковими ключами

---

### РОЗДІЛ 6. КОПІЮВАННЯ, ГЕНЕРАТОРИ ТА БАР'ЄРИ ПРОДУКТИВНОСТІ

**51. У чому полягає різниця між присвоєнням `B = A` та поверхневим копіюванням `B = A.copy()` для списку `A`?**
* а) Присвоєння створює новий псевдонім (alias) на той самий список; `copy()` створює новий `PyListObject` з дублікатом масиву покажчиків
* б) Присвоєння створює глибоку копію, а `copy()` — поверхневу
* в) `copy()` виділяє пам'ять під дані елементів, а присвоєння — тільки під заголовок
* г) Між ними немає жодної практичної або структурної відмінності

**52. Яку роль відіграє внутрішній словник `memo` у роботі функції `copy.deepcopy()`?**
* а) Стискає великі двійкові об'єкти перед записом у пам'ять
* б) Зберігає відповідність `id(original) -> cloned` для запобігання зацикленню при циклічних посиланнях
* в) Кешує результати роботи генераторів списків
* г) Зберігає копії рядків у системному пулі інтернування

**53. Які критичні ризики та обмеження пов'язані з використанням `copy.deepcopy()`? *(Оберіть усі правильні відповіді)***
* а) Значне падіння швидкодії через накладні витрати на інспекцію графа об'єктів
* б) Ризик виникнення `RecursionError` при великій глибині вкладеності об'єктів
* в) Неможливість копіювання дескрипторів сокетів, відкритих файлів та РОЗДІЛувань потоків
* г) Автоматичне перетворення всіх вкладених кортежів на мутабельні списки
* д) Втрата значень цілочисельних елементів через переповнення мантиси

**54. Модифікація вкладеного змінюваного списку `B[0].append(99)` у поверхневій копії `B = A[:]` гарантовано не змінює початковий список `A`.**
* а) Правильно
* б) Неправильно

**55. Які значення міститиме список `matrix` після виконання коду:**
```python
row = [1, 2]
matrix = [row] * 3
matrix[0] = [8, 9]
matrix[1][0] = 99
```
* а) `[[8, 9], [99, 2], [99, 2]]`
* б) `[[99, 9], [99, 2], [99, 2]]`
* в) `[[8, 9], [99, 2], [1, 2]]`
* г) `[[8, 9], [1, 2], [1, 2]]`

**56. Яка спеціалізована інструкція байт-коду забезпечує високу швидкість виконання спискових включень (List Comprehensions) в обхід протоколу виклику методів?**
* а) `BUILD_LIST`
* б) `CALL_METHOD_FAST`
* в) `LIST_APPEND`
* г) `ARRAY_PUSH`

**57. Чому спискове включення `[x*x for x in data]` працює на 25-40% швидше за імперативний цикл `for` із викликом `result.append()`?**
* а) Усуваються накладні витрати на динамічний пошук атрибута `LOAD_ATTR` і побудову кадру виклику функції
* б) Спискове включення виконується у фоновому потоці ядра операційної системи
* в) Компілятор автоматично транслює вираз у нативні векторні інструкції AVX-512
* г) Спискове включення не створює об'єкти в купі, зберігаючи їх у регістрах ЦП

**58. Який головний інженерний ризик пов'язаний із жадібним обчисленням (eager evaluation) великих спискових включень?**
* а) Зміна типів даних числових результатів на float64
* б) Неможливість використання умовних фільтрів `if` всередині виразу
* в) Автоматичне РОЗДІЛування виконання інших процесів м'ютексом GIL
* г) Ризик вичерпання оперативної пам'яті (`MemoryError`) через одномоментне виділення масиву покажчиків

**59. Чому послідовний перебір елементів списку Python спричиняє промахи кешу (CPU cache misses) на відміну від масивів мови C чи NumPy?**
* а) Тому що CPython вимикає кеш L1 під час роботи віртуальної машини PVM
* б) Ефект «гонитви за покажчиками» (pointer chasing): масив містить лише адреси, а самі дані розпорошені в купі
* в) Через те, що розмір покажчика перевищує ширину стандартної кеш-лінії процесора
* г) Тому що списки в Python ніколи не кешуються контролером оперативної пам'яті

**60. Яку структуру даних зі стандартної бібліотеки Python рекомендовано використовувати замість списку для побудови FIFO-черги з операціями вставки та вилучення за час $O(1)$?**
* а) `collections.deque`
* б) `collections.namedtuple`
* в) `array.array`
* г) `queue.PriorityQueue`

## Тема 4

### РОЗДІЛ 1. АРХІТЕКТУРА ТИПУ STR, UNICODE ТА PEP 393

**1. Згідно зі специфікацією PEP 393 (Flexible String Representation), яким чином інтерпретатор CPython оптимізує використання пам'яті для рядків типу `str`?**
* А) Адаптивно обирає ширину символу (1, 2 або 4 байти) залежно від максимальної кодової точки в рядку
* Б) Завжди стискає всі рядки за алгоритмом gzip у динамічній пам'яті
* В) Використовує виключно 4 байти (UCS-4) для кожного символу будь-якого рядка
* Г) Зберігає всі рядки у вигляді двозв'язних списків однобайтних символів ASCII

**2. Яку кількість байтів на один символ виділяє CPython для рядка `str`, що містить виключно символи з кодовими точками до U+007F (діапазон ASCII)?**
* А) 1 байт
* Б) 2 байти
* В) 4 байти
* Г) 8 байтів

**3. Що відбувається з внутрішнім представленням рядка ASCII довжиною 100 символів у CPython, якщо до нього додати один символ емодзі (кодова точка вище U+FFFF)?**
* А) Усі символи рядка розгортаються у 4-байтове представлення (UCS-4)
* Б) Рядок розбивається на два незалежних об'єкти з різною шириною символів
* В) Тільки доданий символ займає 4 байти, а решта залишаються 1-байтовими
* Г) Символ емодзі автоматично відкидається через невідповідність кодуванню ASCII

**4. Якою є максимальна допустима кодова точка стандарту Unicode у мові Python?**
* А) U+10FFFF
* Б) U+00FFFF
* В) U+FFFFFF
* Г) U+7FFFFFFF

**5. Рядки типу `str` у мові Python є змінюваними (mutable) структурами даних, що дозволяє виконувати пряме присвоєння `s[0] = 'A'`.**
* А) Правда
* Б) Неправда

**6. Яка C-структура використовується в ядрі CPython для представлення компактних рядків, що складаються виключно із символів ASCII?**
* А) `PyASCIIObject`
* Б) `PyCompactUnicodeObject`
* В) `PyBytesObject`
* Г) `PyStringNode`

**7. Які фіксовані розміри символів (у байтах) підтримує внутрішній механізм гнучкого представлення рядків PEP 393 у CPython?** *(Оберіть усі правильні варіанти)*
* А) 1 байт (Latin-1 / ASCII)
* Б) 2 байти (UCS-2 / BMP)
* В) 3 байти (UTF-8 packed)
* Г) 4 байти (UCS-4 / Astral)
* Д) 8 байтів (64-bit wide)

**8. Яку назву має міжнародний стандарт кодування символів, послідовністю абстрактних кодових точок якого є тип `str` у Python?** *(Дайте коротку текстову відповідь)*

**9. Чому у формулі розрахунку пам'яті рядка $S_{str} = S_{header} + (L \cdot w) + w$ присутній додатковий доданок $w$ наприкінці?**
* А) Для збереження завершального нульового символу відповідної ширини для прямої сумісності з C-бібліотеками
* Б) Для збереження контрольної суми CRC32 рядка
* В) Для вирівнювання адреси пам'яті за 64-розрядною межею
* Г) Для збереження довжини рядка

**10. Встановіть відповідність між категорією символів та шириною комірки пам'яті для їх зберігання в CPython згідно з PEP 393:**
1. Символи ASCII (до U+007F)
2. Символи кирилиці та грецької абетки (U+0100 до U+FFFF)
3. Символи емодзі та розширені гліфи (U+10000 і вище)
---
* А) 1 байт
* Б) 2 байти
* В) 4 байти

**11. Рядок, що містить виключно літери українського алфавіту (діапазон базової багатомовної площини BMP), виділяє в CPython по 2 байти на кожен символ.**
* А) Правда
* Б) Неправда

**12. Завдяки якій властивості типу `str` значення його хеш-коду (поле `ob_shash`) може безпечно обчислюватися один раз і кешуватися у заголовку об'єкта?**
* А) Незмінності (immutability) рядка
* Б) Гнучкому представленню PEP 393
* В) Використанню буферного протоколу
* Г) Динамічній типізації мови Python

---

### РОЗДІЛ 2. ІНДЕКСАЦІЯ ТА ЗРІЗИ РЯДКІВ

**13. Якою є часова складність отримання одного символу за індексом `s[i]` у рядку типу `str` у Python?**
* А) $O(1)$
* Б) $O(n)$
* В) $O(\log n)$
* Г) $O(n^2)$

**14. Чому CPython забезпечує константний час доступу $O(1)$ до довільного символу рядка `str` за його індексом?**
* А) Усі символи конкретного рядка мають строго однакову ширину в байтах, що забезпечує пряме адресне зміщення вказівника
* Б) Інтерпретатор використовує префіксне дерево (Trie) для пошуку символів
* В) Кожен символ зберігається як окремий ключ у внутрішній хеш-таблиці
* Г) Усі рядки заздалегідь завантажуються в регістри процесора

**15. За якою формулою обчислюється фактичний індекс для від'ємного значення індексу `i` у рядку довжиною `L`?**
* А) $L + i$
* Б) $L - i$
* В) $|i| - 1$
* Г) $L + i - 1$

**16. Якою є часова та просторова складність операції взяття зрізу `s[start:stop]` довжиною $K$ для рядка типу `str`?**
* А) $O(K)$ за часом та $O(K)$ за пам'яттю
* Б) $O(1)$ за часом та $O(1)$ за пам'яттю
* В) $O(N)$ за часом та $O(1)$ за пам'яттю
* Г) $O(N^2)$ за часом та $O(K)$ за пам'яттю

**17. Якщо значення індексів `start` або `stop` у зрізі рядка `s[start:stop]` виходять за фактичні межі довжини рядка, Python генерує виняток `IndexError`.**
* А) Правда
* Б) Неправда

**18. Який виняток виникає при спробі отримати символ за індексом `s[len(s)]` для непорожнього рядка `s`?**
* А) `IndexError`
* Б) `ValueError`
* В) `KeyError`
* Г) `OverflowError`

**19. Які значення за замовчуванням мають межі `start` та `stop` при взятті зрізу `s[::-1]` для рядка довжиною `L`?**
* А) $\text{start} = L - 1$, $\text{stop} = -(L + 1)$
* Б) $\text{start} = 0$, $\text{stop} = L$
* В) $\text{start} = L$, $\text{stop} = 0$
* Г) $\text{start} = -1$, $\text{stop} = 0$

**20. Операція взяття зрізу над рядком типу `str` завжди створює новий об'єкт у пам'яті з фізичним копіюванням відповідних символів.**
* А) Правда
* Б) Неправда

---

### РОЗДІЛ 3. ESCAPE-ПОСЛІДОВНОСТІ ТА СИРІ РЯДКИ

**21. За допомогою якого префікса перед рядковим літералом створюється «сирий» рядок (raw string) у мові Python?**
* А) `r` або `R`
* Б) `b` або `B`
* В) `f` або `F`
* Г) `u` або `U`

**22. У чому полягає особливість сирого рядкового літерала `r"path\to\new"`?**
* А) Символи зворотного слешу не інтерпретуються як початок керувальних escape-послідовностей
* Б) Рядок автоматично конвертується у двійковий масив байтів
* В) Рядок розміщується у спільній пам'яті операційної системи
* Г) Рядок стає змінюваним (mutable) об'єктом

**23. Яке синтаксичне обмеження накладає мова Python на сирі рядкові літерали?**
* А) Сирий рядок не може закінчуватися непарною кількістю зворотних слешів
* Б) Сирий рядок не може містити цифри та символи пунктуації
* В) Сирий рядок повинен містити виключно символи ASCII
* Г) Сирий рядок не підтримує взяття зрізів

**24. Якими керувальними escape-послідовностями та шістнадцятковими кодами ASCII позначається послідовність повернення каретки та нового рядка (CRLF)?**
* А) `\r\n` (0x0D та 0x0A)
* Б) `\n\r` (0x0A та 0x0D)
* В) `\t\n` (0x09 та 0x0A)
* Г) `\x00\x01` (0x00 та 0x01)

**25. Довжина сирого рядка `len(r"\n")` дорівнює 2, тоді як довжина звичайного рядка `len("\n")` дорівнює 1.**
* А) Правда
* Б) Неправда

**26. За допомогою якої escape-послідовності можна задати символ у рядку Python за його однобайтним шістнадцятковим числовим кодом?**
* А) `\xHH`
* Б) `\uHHHH`
* В) `\UHHHHHHHH`
* Г) `\oOOO`

---

### РОЗДІЛ 4. ДВІЙКОВІ ТИПИ ТА БУФЕРНИЙ ПРОТОКОЛ

**27. Значення яких числових діапазонів може приймати кожен окремий елемент незмінної двійкової послідовності `bytes`?**
* А) Цілі числа від 0 до 255 включно
* Б) Цілі числа від -128 до 127 включно
* В) Цілі числа від 0 до 65535 включно
* Г) Будь-які кодові точки стандарту Unicode

**28. Який тип даних повертає операція вилучення одного елемента `b[0]` для об'єкта `b = b"ABC"`?**
* А) `int`
* Б) `bytes`
* В) `str`
* Г) `bytearray`

**29. Яка головна відмінність типу `bytearray` від типу `bytes`?**
* А) `bytearray` є змінюваною (mutable) послідовністю і підтримує модифікацію елементів за місцем
* Б) `bytearray` містить лише 2-байтові символи Unicode
* В) `bytearray` підтримує лише читання даних без можливості взяття зрізів
* Г) `bytearray` є незмінною послідовністю зі стисненням даних

**30. Об'єкт типу `bytearray` може бути використаний як ключ у словнику (`dict`), оскільки він зберігає двійкові байти.**
* А) Правда
* Б) Неправда

**31. Який виняток згенерує інтерпретатор при спробі створити об'єкт `bytes([10, 20, 300])`?**
* А) `ValueError`
* Б) `TypeError`
* В) `IndexError`
* Г) `OverflowError`

**32. Яке головне призначення об'єкта `memoryview` у мові Python?**
* А) Надання інтерфейсу доступу до буфера пам'яті об'єкта без фізичного копіювання байтів (Zero-Copy)
* Б) Стиснення двійкових даних перед передачею в мережевий сокет
* В) Автоматичний переклад символів Unicode у системне кодування ОС
* Г) Захист пам'яті процесу від збирача сміття

**33. Якою є просторова та часова складність взяття зрізу над об'єктом `memoryview`?**
* А) $O(1)$ за часом та $O(1)$ за пам'яттю
* Б) $O(K)$ за часом та $O(K)$ за пам'яттю
* В) $O(N)$ за часом та $O(1)$ за пам'яттю
* Г) $O(N^2)$ за часом та $O(N)$ за пам'яттю

**34. Поки існує активний екземпляр `memoryview`, створений над `bytearray`, розмір вихідного `bytearray` заборонено змінювати (наприклад, викликати `append` або `pop`).**
* А) Правда
* Б) Неправда

**35. Встановіть відповідність між типом даних та його ключовою архітектурною властивістю:**
1. `bytes`
2. `bytearray`
3. `memoryview`
4. `str`
---
* А) Незмінна послідовність октетних значень, підтримує хешування
* Б) Змінювана послідовність октетних значень із динамічним овер-алокатором
* В) Дескриптор буферного протоколу з нульовим копіюванням пам'яті
* Г) Незмінна послідовність абстрактних кодових точок Unicode

**36. Які інженерні переваги надає використання `memoryview` при обробці великих масивів бінарних даних?** *(Оберіть усі правильні варіанти)*
* А) Усунення накладних витрат на виділення оперативної пам'яті під час взяття зрізів
* Б) Підвищення пропускної здатності конвеєра за рахунок відсутності фізичного дублювання байтів
* В) Автоматичне шифрування даних за алгоритмом AES-256
* Г) Можливість модифікувати незмінні об'єкти типу `bytes` безпосередньо у пам'яті

**37. Яка технологія ядра Python (специфікація PEP 3118) дозволяє об'єктам `memoryview` напряму взаємодіяти з низькорівневою пам'яттю інших контейнерів?** *(Дайте коротку текстову відповідь)*

**38. Що станеться за замовчуванням при спробі виконати декодування `b"\xFF\xFE".decode('utf-8')`?**
* А) Виникне виняток `UnicodeDecodeError`
* Б) Байти автоматично заміняться на символи пробілу
* В) Рядок успішно декодується у формат ASCII
* Г) Інтерпретатор аварійно завершить роботу із Segmentation Fault

---

### РОЗДІЛ 5. МЕТОДИ РЯДКІВ ТА КОНКАТЕНАЦІЯ

**39. Яку асимптотичну часову складність має конкатенація рядків за допомогою оператора `s += chunk` у циклі з $M$ ітерацій (де довжина кожного фрагмента $L$) у загальному випадку?**
* А) $O(L \cdot M^2)$
* Б) $O(L \cdot M)$
* В) $O(M \log M)$
* Г) $O(1)$

**40. Чому метод `str.join(iterable)` працює з лінійною складністю $O(N)$ на відміну від конкатенації оператором додавання у циклі?**
* А) Попередньо обчислює сумарну довжину, виділяє буфер пам'яті один раз і переносить байти через `memcpy`
* Б) Використовує апаратні обчислення на відеокарті (GPU)
* В) Автоматично перетворює всі рядки у двійковий формат без виділення пам'яті
* Г) Створює генераторний вираз, який відкладає обчислення

**41. Як функціонує метод `s.split()` без явної передачі роздільника (`sep=None`)?**
* А) Розбиває рядок за довільною кількістю поспіль розташованих пробільних символів, ігноруючи початкові та кінцеві пробіли
* Б) Розбиває рядок строго за кожним одиничним символом пробілу
* В) Розбиває рядок на список окремих символів
* Г) Генерує виняток `ValueError`

**42. Скільки елементів міститиме результуючий список після виконання виразу `"a:b:c:d".split(":", maxsplit=2)`?**
* А) 3
* Б) 2
* В) 4
* Г) 1

**43. Метод `s.replace(old, new)` модифікує вихідний рядок `s` за місцем без виділення нового РОЗДІЛу пам'яті.**
* А) Правда
* Б) Неправда

**44. Які твердження щодо методів `strip()`, `lstrip()`, `rstrip()` є правильними?** *(Оберіть усі правильні варіанти)*
* А) Якщо набір символів не передано, вони видаляють пробільні символи (пробіли, табуляції, розриви рядків)
* Б) Вони повертають новий рядок і не змінюють вихідний об'єкт
* В) Вони видаляють лише перше поодиноке входження вказаного символу
* Г) Вони змінюють внутрішнє кодування рядка на чистий ASCII

---

### РОЗДІЛ 6. ІНТЕРПОЛЬОВАНІ F-РЯДКИ ТА ФОРМАТУВАННЯ

**45. Які спеціалізовані інструкції байт-коду CPython генеруються компілятором для обробки інтерпольованих f-рядків?**
* А) `FORMAT_VALUE` та `BUILD_STRING`
* Б) `CALL_METHOD` та `LOAD_ATTR`
* В) `STR_CONCAT` та `ALLOC_STRING`
* Г) `PRINT_ITEM` та `POP_TOP`

**46. Який результат поверне форматування цілого числа виразом `f"{254:#06X}"` у Python?**
* А) `0x00FE`
* Б) `0xFE`
* В) `0000FE`
* Г) `FE0000`

**47. Який специфікатор форматування у f-рядку слід застосувати для виведення дійсного числа `x` із фіксацією рівно трьох знаків після коми?**
* А) `{x:.3f}`
* Б) `{x:3d}`
* В) `{x:#3f}`
* Г) `{x:3s}`

**48. Який символ у специфікаторі форматування Python відповідає за вирівнювання значення по центру поля заданої ширини?**
* А) `^`
* Б) `<`
* В) `>`
* Г) `=`

**49. Усередині інтерполяційних полів f-рядка дозволено виконувати довільні валідні вирази мови Python, включаючи виклики функцій та арифметичні операції.**
* А) Правда
* Б) Неправда

**50. Який символ типу у специфікаторі форматування переводить дійсне число у стандартну експоненційну (наукову) нотацію?**
* А) `e` (або `E`)
* Б) `f`
* В) `d`
* Г) `x`

---

### РОЗДІЛ 7. РЕГУЛЯРНІ ВИРАЗИ ТА МОДУЛЬ RE

**51. Чому рекомендується викликати `re.compile()` перед багаторазовим використанням регулярного виразу в циклі?**
* А) Шаблон транслюється у байт-код автомата один раз, усуваючи накладні витрати на повторний синтаксичний аналіз
* Б) Скомпільований шаблон автоматично виконується в окремому процесі ОС
* В) Це дозволяє обійти обмеження пам'яті на довжину тексту
* Г) `re.compile()` автоматично виправляє синтаксичні помилки у шаблоні

**52. Який символ після квантифікатора (наприклад, `.*` або `+`) перетворює його з жадібного режиму на лінивий (reluctant)?**
* А) `?`
* Б) `+`
* В) `*`
* Г) `!`

**53. У чому полягає принципова відмінність між методами `re.match()` та `re.search()`?**
* А) `re.match()` перевіряє відповідність строго від початку рядка, а `re.search()` сканує рядок у пошуку першого збігу
* Б) `re.match()` повертає всі збіги, а `re.search()` — лише перший
* В) `re.match()` працює лише з двійковими даними, а `re.search()` — лише з текстом
* Г) `re.match()` не підтримує іменовані групи захоплення

**54. Який синтаксис використовується для створення іменованої групи захоплення у регулярних виразах модуля `re`?**
* А) `(?P<group_name>pattern)`
* Б) `(?<group_name>pattern)`
* В) `(?group_name:pattern)`
* Г) `[group_name=pattern]`

**55. Які прапорці компіляції підтримує модуль `re`?** *(Оберіть усі правильні варіанти)*
* А) `re.IGNORECASE` (ігнорування регістру символів)
* Б) `re.MULTILINE` (зміна поведінки якорів `^` та `$` на рівень кожного рядка)
* В) `re.DOTALL` (дозволяє символу крапки `.` узгоджуватися з символом переведення рядка)
* Г) `re.FASTMODE` (вимикання перевірки типів)
* Д) `re.PARALLEL` (виконання на всіх ядрах ЦП)

**56. Яка головна перевага використання `re.finditer()` порівняно з `re.findall()` при обробці великих обсягів тексту?**
* А) `re.finditer()` повертає генераторний ітератор об'єктів `Match` ліниво, не навантажуючи пам'ять повним списком
* Б) `re.finditer()` працює швидше за рахунок використання алгоритму Дейкстри
* В) `re.finditer()` підтримує автоматичну фільтрацію спаму
* Г) `re.finditer()` повертає виключно текстові рядки без метаданих

---

### РОЗДІЛ 8. ГРАНИЧНІ УМОВИ, REDOS ТА ПРИКЛАДНІ КЕЙСИ

**57. Яка головна структурна причина виникнення катастрофічного зворотного відстеження (Catastrophic Backtracking / ReDoS) у рушіях регулярних виразів типу backtracking NFA?**
* А) Наявність вкладених квантифікаторів над символьними класами, що перетинаються (наприклад, `(a+)+`)
* Б) Переповнення внутрішнього стека компілятора `re` через довжину рядка понад 64 КБ
* В) Використання іменованих груп замість числових індексів
* Г) Спроба пошуку символів Unicode у сирих рядках `r"..."`

**58. До якої асимптотичної часової складності відносно довжини вхідного рядка $n$ призводить явище катастрофічного зворотного відстеження у найгіршому випадку?** *(Вкажіть складність у $O$-нотації)*

**59. Чому пряме порівняння двох візуально однакових рядків Unicode з діакритичними знаками через оператор `==` може повернути `False` без попередньої нормалізації (`unicodedata.normalize`)?**
* А) Один рядок може бути представлений у прекомпонованій формі (NFC), а інший — у декомпонованій (NFD, базова літера + окремий модифікатор)
* Б) Python автоматично шифрує рядки різними відкритими ключами
* В) Діакритичні знаки завжди видаляються синтаксичним аналізатором
* Г) Оператор `==` у Python не підтримує символи за межами кодування ASCII

**60. За яким математичним алгоритмом обчислюється контрольна сума інформаційного кадру NMEA-0183 між символами `$` та `*`?**
* А) Побітове додавання за модулем два (XOR) усіх байтів сегмента
* Б) Арифметична сума байтів за модулем 256 (LRC)
* В) Циклічний надлишковий код CRC-32
* Г) Криптографічний хеш MD5

## Тема 5

#### РОЗДІЛ 1. МАТЕМАТИЧНІ ЗАСАДИ ХЕШУВАННЯ ТА КОНТРАКТ ХЕШОВАНОСТІ

**1. Яке математичне твердження описує обов'язковий контракт хешованості (hashability contract) у мові Python?**
* А) `(x == y) ==> (hash(x) == hash(y))`
* Б) `(hash(x) == hash(y)) ==> (x == y)`
* В) `(x != y) ==> (hash(x) != hash(y))`
* Г) `(x == y) <==> (hash(x) == hash(y))`

**2. Які з наведених типів даних можуть бути використані як ключі словника `dict` у Python? (Оберіть усі правильні відповіді)**
* А) `str`
* Б) `frozenset`
* В) `tuple`, що містить тільки незмінювані типи
* Г) `list`
* Д) `set`

**3. У мові Python вираз `hash(1) == hash(1.0)` повертає `True`, оскільки значення `1` та `1.0` рівні за оператором порівняння `==`.**
* А) Правда (True)
* Б) Неправда (False)

**4. Чому змінювані (mutable) об'єкти, такі як списки `list`, заборонено використовувати як ключі у хеш-таблицях Python?**
* А) Зміна внутрішнього стану об'єкта змінює його хеш або порушує рівність, роблячи його недосяжним у таблиці
* Б) Змінювані об'єкти завжди займають понад 1024 байти у пам'яті
* В) Інтерпретатор CPython не може виділити покажчик на змінюваний об'єкт
* Г) Метод відкритої адресації працює виключно з цілими числами

**5. Який виняток збуджує інтерпретатор CPython при спробі виконати операцію `d = {[1, 2]: "data"}`?**
* А) `TypeError`
* Б) `KeyError`
* В) `ValueError`
* Г) `AttributeError`

**6. Як за замовчуванням обчислюється хеш-код для екземпляра користувацького класу, якщо методи `__hash__` та `__eq__` не були явно перевизначені?**
* А) На основі фізичної адреси об'єкта в оперативній пам'яті (`id`)
* Б) Як константа 0 для всіх екземплярів класу
* В) Шляхом обчислення хешу від конкатенації імен атрибутів
* Г) Користувацькі класи не мають хеш-коду за замовчуванням і збуджують `TypeError`

**7. Чи може кортеж вигляду `(1, 2, [3, 4])` використовуватися як ключ словника `dict`?**
* А) Ні, оскільки він містить змінюваний елемент `list`, що робить увесь кортеж нехешованим
* Б) Так, оскільки сам тип `tuple` є незмінюваним
* В) Так, але тільки якщо список `[3, 4]` не модифікуватиметься
* Г) Ні, кортежі взагалі не можуть бути ключами словників

**8. Які два обов'язкові спеціальні методи (дандер-методи) повинен коректно реалізовувати об'єкт для підтримки контракту хешованості?**
*(Дайте коротку відповідь самостійно)*

**9. Яку розрядність має цілочисельне значення хеш-коду, що повертається вбудованою функцією `hash()` на 64-бітній платформі CPython?**
* А) 32 біти
* Б) 64 біти
* В) 128 бітів
* Г) 256 бітів

**10. Встановіть відповідність між типом даних у Python та його характеристикою хешованості:**

*Типи даних:*
1. `list`
2. `str`
3. `tuple`
4. `dict`

*Характеристики:*
* А) Завжди хешований (hashable)
* Б) Хешований лише за умови хешованості всіх його елементів
* В) Завжди нехешований (unhashable)

**11. За якою формулою інтерпретатор CPython здійснює швидке відображення 64-бітного хеш-коду $h(k)$ на індекс кошика таблиці розміром $M = 2^m$?**
* А) `h(k) & (M - 1)`
* Б) `h(k) % (M + 1)`
* В) `h(k) >> M`
* Г) `h(k) ^ (M - 1)`

**12. Значення `hash("admin")` завжди залишається незмінним між різними перезапусками процесу інтерпретатора Python завдяки глобальній константі алгоритму.**
* А) Правда (True)
* Б) Неправда (False)

---

#### РОЗДІЛ 2. ВНУТРІШНЯ АРХІТЕКТУРА КОМПАКТНОГО СЛОВНИКА

**13. З яких двох фізично розділених таблиць складається внутрішня архітектура компактного словника (Compact Dict) у CPython? (Оберіть дві правильні відповіді)**
* А) Розріджений масив індексів (`dk_indices`)
* Б) Щільний масив фактичних записів (`dk_entries`)
* В) Зв'язаний список переповнення (`dk_overflow`)
* Г) Бінарне дерево колізій (`dk_tree`)

**14. Яке функціональне призначення масиву `dk_indices` у компактному словнику CPython?**
* А) Трансляція замаскованого хеш-коду в порядковий індекс елемента в масиві `dk_entries`
* Б) Збереження копій об'єктів значень (values)
* В) Кешування відсортованого списку ключів словника
* Г) Синхронізація РОЗДІЛувань між системними потоками

**15. Який тип даних використовує CPython для елементів масиву `dk_indices` у невеликих словниках місткістю до 128 слотів?**
* А) `int8_t`
* Б) `int16_t`
* В) `int32_t`
* Г) `int64_t`

**16. Які три поля містить кожна структура `PyDictKeyEntry` у щільному масиві `dk_entries`?**
* А) 64-бітний хеш ключа, покажчик на об'єкт ключа, покажчик на об'єкт значення
* Б) Покажчик на наступний кошик, покажчик на попередній кошик, значення
* В) Хеш-код ключа, прапорець видалення, покажчик на ключ
* Г) Індекс кошика, розмір об'єкта, лічильник посилань

**17. Починаючи з версії Python 3.7, збереження хронологічного порядку вставки елементів у словнику `dict` є гарантованим стандартом мови.**
* А) Правда (True)
* Б) Неправда (False)

**18. За якого граничного коефіцієнта заповнення (load factor) таблиці інтерпретатор CPython ініціює процедуру динамічного розширення (resizing) словника?**
* А) Рівно 1/2 (50%)
* Б) Близько 2/3 (приблизно 66%)
* В) Рівно 3/4 (75%)
* Г) При повному заповненні (100%)

**19. Приблизно який відсоток оперативної пам'яті дозволила зекономити компактна архітектура словників Хеттінгера порівняно зі старою розрідженою моделлю?**
* А) 5% - 10%
* Б) 20% - 25%
* В) 50% - 60%
* Г) Понад 80%

**20. Встановіть відповідність між компонентом компактного словника та його характеристикою:**

*Компоненти:*
1. `dk_indices`
2. `dk_entries`
3. `Load Factor`
4. `Dummy`

*Характеристики:*
* А) Щільний масив структур фіксованого розміру (24 байти кожна)
* Б) Розріджений масив, що містить цілі числа розміром від 1 до 8 байтів
* В) Спеціальний маркер для позначення видаленого запису
* Г) Відношення кількості активних записів до загальної місткості таблиці

**21. Впорядкованість сучасного словника `dict` у Python означає, що його ключі автоматично сортуються за зростанням під час додавання.**
* А) Правда (True)
* Б) Неправда (False)

**22. Хто з ключових розробників CPython запропонував архітектуру компактного словника, реалізовану в PEP 468?**
* А) Гвідо ван Россум
* Б) Реймонд Хеттінгер
* В) Тім Петерс
* Г) Бретт Кеннон

---

#### РОЗДІЛ 3. РОЗВ'ЯЗАННЯ КОЛІЗІЙ ТА ПСЕВДОВИПАДКОВЕ ЗОНДУВАННЯ

**23. Яку загальну стратегію розв'язання колізій хешування використовує інтерпретатор CPython для словників та множин?**
* А) Відкрита адресація (Open Addressing)
* Б) Метод ланцюжків (Separate Chaining)
* В) Ідеальне хешування (Perfect Hashing)
* Г) Хешування зозулею (Cuckoo Hashing)

**24. Чому розробники CPython обрали відкриту адресацію замість методу зв'язаних списків (ланцюжків)?**
* А) Забезпечує кращу просторову локальність даних і високу ефективність процесорного кешу L1/L2
* Б) Повністю усуває можливість виникнення колізій хешування
* В) Дозволяє таблиці заповнюватися на 100% без потреби розширення
* Г) Не потребує використання оператора рівності `__eq__` при пошуку

**25. Чому у CPython відмовилися від використання простого лінійного зондування (linear probing)?**
* А) Спричиняє ефект первинної кластеризації, деградуючи час пошуку до $O(N)$
* Б) Не дозволяє адресифікувати таблиці розміром більше 256 слотів
* В) Вимагає знаходження оберненого елемента в полі Галуа
* Г) Працює виключно з числовими ключами

**26. Які компоненти входять до рекурентної формули псевдовипадкового зондування CPython: `j = (5*j + 1 + perturb) % M`? (Оберіть усі правильні відповіді)**
* А) Множник 5 (взаємно простий із розміром степеня двійки $2^m$)
* Б) Параметр збурення `perturb`, що ініціалізується повним значенням хешу
* В) Розмір індексної таблиці $M$, що є степенем двійки
* Г) Кількість видалених слотів `Dummy`
* Д) Поточний лічильник посилань об'єкта

**27. Яка властивість множника 5 є критичною у формулі псевдовипадкового зондування CPython?**
* А) Він зменшує розмір кошика в 5 разів
* Б) Він є взаємно простим із розміром таблиці $2^m$, що гарантує повний обхід усіх кошиків
* В) Він відповідає кількості ядер сучасного процесора
* Г) Він примусово обмежує кількість колізій п'ятьма спробами

**28. Яку функцію виконує побітовий зсув `perturb = perturb >> 5` на кожному кроці зондування?**
* А) Поступово підмішує старші розряди початкового хеш-коду, розсіюючи колізійні траєкторії
* Б) Обнуляє молодші біти індексу для переходу в інший сегмент пам'яті
* В) Визначає момент, коли необхідно викликати збирач сміття
* Г) Контролює кількість вільних байтів в оперативній пам'яті

**29. Встановіть відповідність між числовим маркером у масиві `dk_indices` та станом слота:**

*Числові маркери:*
1. `-1`
2. `-2`
3. Позитивне число або `0`

*Стани слота:*
* А) `Active` (активний слот, що вказує на запис у `dk_entries`)
* Б) `Dummy` (фіктивний слот, з якого елемент було видалено)
* В) `Unused` (вільний порожній слот)

**30. Чому видалений елемент у хеш-таблиці з відкритою адресацією позначається маркером `Dummy` замість повернення у стан `Unused`?**
* А) Для збереження копії значення на випадок скасування операції
* Б) Щоб не обірвати ланцюжок зондування для інших ключів, які зазнали колізії з цим слотом
* В) Цього вимагає стандарт кодування UTF-8
* Г) Щоб запобігти зменшенню розміру таблиці в пам'яті

**31. Коли відбувається повне очищення та утилізація слотів зі статусом `Dummy`?**
* А) Під час процедури зміни розміру та рехешування (resizing) таблиці
* Б) При кожному виклику оператора `del`
* В) Щойно кількість `Dummy` перевищує 5 штук
* Г) Під час кожного виклику функції `len()`

**32. Який криптографічний алгоритм із рандомізацією зерна (seed) було інтегровано в CPython для захисту словників від атак типу HashDoS?**
* А) MD5
* Б) SipHash
* В) SHA-256
* Г) AES-GCM

---

#### РОЗДІЛ 4. АРХІТЕКТУРА МНОЖИН: SET ТА FROZENSET

**33. Структура даних `set` у CPython використовує розділені масиви (Compact Architecture) аналогічно до словника `dict`.**
* А) Правда (True)
* Б) Неправда (False)

**34. Множина `set` у мові Python зберігає хронологічний порядок вставки елементів під час ітерації.**
* А) Правда (True)
* Б) Неправда (False)

**35. Яка головна відмінність між типами `set` та `frozenset` у мові Python?**
* А) `frozenset` є незмінюваним (immutable) і хешованим, а `set` — змінюваним і нехешованим
* Б) `frozenset` зберігає дублікати елементів, а `set` — ні
* В) `frozenset` використовує алгоритм бінарного дерева пошуку
* Г) `set` дозволяє зберігати списки, а `frozenset` — ні

**36. Чи може екземпляр `frozenset` бути ключем словника `dict` або елементом іншої множини `set`?**
* А) Ні, жодні види множин не можуть бути ключами словників
* Б) Так, оскільки `frozenset` є незмінюваним і реалізує метод `__hash__`
* В) Так, але тільки якщо він містить не більше трьох елементів
* Г) Ні, тому що `frozenset` не підтримує оператор порівняння `==`

**37. З яких полів складається внутрішній запис `setentry` таблиці множини `set` у мові C?**
* А) Покажчик на ключ, покажчик на значення, покажчик на наступний елемент
* Б) Покажчик на об'єкт ключа (`PyObject *key`) та 64-бітне значення хешу (`Py_hash_t hash`)
* В) Хеш-код та прапорець РОЗДІЛування потоку
* Г) Строковий ключ та цілочисельний лічильник кількості входжень

**38. Якщо у множину `s = {1, 2}` додати елемент `2` через метод `s.add(2)`, розмір множини `len(s)` збільшиться до 3.**
* А) Правда (True)
* Б) Неправда (False)

**39. Яку спеціалізовану інструкцію байт-коду PVM використовує для оптимізованого додавання елементів у генераторі множин (Set Comprehension)?**
* А) `SET_ADD`
* Б) `MAP_ADD`
* В) `LIST_APPEND`
* Г) `STORE_SUBSCR`

**40. Встановіть відповідність між структурою даних та її властивостями:**

*Структури даних:*
1. `dict`
2. `set`
3. `frozenset`
4. `list`

*Властивості:*
* А) Впорядкована послідовність, допускає дублікати, пошук за $O(N)$
* Б) Зберігає тільки унікальні ключі, не зберігає порядок вставки
* В) Незмінювана множина, може бути ключем словника
* Г) Зберігає пари ключ-значення, зберігає порядок вставки (Python 3.7+)

---

#### РОЗДІЛ 5. АЛГОРИТМІЧНИЙ ІНТЕРФЕЙС, МЕТОДИ ТА ОПЕРАЦІЇ

**41. У чому полягає інженерна перевага методу `d.get(k, default)` над конструкцією перевірки `if k in d: return d[k]`?**
* А) Працює за час $O(\log N)$ замість $O(N)$
* Б) Виконує пошук та обчислення хешу один раз замість двох, уникаючи зайвих операцій та стану гонитви
* В) Повністю вимикає обчислення хеш-функції для ключа
* Г) Автоматично перетворює ключ на тип `string`

**42. Що повертає виклик `d.setdefault("port", 8080)`, якщо ключ `"port"` вже існує у словнику зі значенням `443`?**
* А) `8080` (значення перезаписується)
* Б) `443` (існуюче значення не змінюється)
* В) `None`
* Г) Збуджує виняток `KeyError`

**43. Який виняток виникає при спробі додати новий ключ у словник безпосередньо під час ітерації по ньому в циклі `for`?**
* А) `IndexError`
* Б) `KeyError`
* В) `RuntimeError`
* Г) `StopIteration`

**44. Які характеристики притаманні об'єктам уявлень словника (`dict_keys`, `dict_items`)? (Оберіть усі правильні відповіді)**
* А) Відображають зміни у базовому словнику динамічно без додаткового копіювання
* Б) Підтримують теоретико-множинні операції (перетин, об'єднання)
* В) Виділяють повну копію списку ключів в оперативній пам'яті
* Г) Дозволяють звертатися до елементів за числовим індексом `d.keys()[0]`

**45. У чому полягає синтаксична різниця між бінарними операторами множин (наприклад, `A | B`) та їхніми іменованими методами (наприклад, `A.union(B)`)?**
* А) Оператори вимагають, щоб обидва операнди були множинами, а методи приймають будь-які ітеровані об'єкти
* Б) Методи працюють за лінійний час, а оператори — за логарифмічний
* В) Оператори модифікують множину на місці, а методи завжди повертають нову множину
* Г) Між ними немає жодної різниці, це абсолютні синоніми

**46. Який символ мови Python реалізує операцію симетричної різниці множин (елементи, що належать або $A$, або $B$, але не обом одночасно)?**
* А) `^`
* Б) `&`
* В) `|`
* Г) `-`

**47. Як CPython оптимізує операцію перетину двох множин `A.intersection(B)`?**
* А) Завжди ітерує множину `B` незалежно від її розміру
* Б) Сортує обидві множини за зростанням і застосовує бінарний пошук
* В) Ітерує елементи меншої за розміром множини, виконуючи пошук за хешем у більшій
* Г) Перетворює обидві множини на бітові вектори фіксованої довжини

**48. Який результат поверне вираз `{1, 2, 3, 4} ^ {3, 4, 5, 6}`?**
* А) `{3, 4}`
* Б) `{1, 2, 5, 6}`
* В) `{1, 2}`
* Г) `{1, 2, 3, 4, 5, 6}`

**49. Який вираз коректно інвертує словник `d = {"a": 1, "b": 2}` (міняє місцями ключі та значення)?**
* А) `{k: v for v, k in d.items()}`
* Б) `[v: k for k, v in d]`
* В) `{v: k for k, v in d.items()}`
* Г) `{v == k for k, v in d.items()}`

**50. Встановіть відповідність між бінарним оператором над множинами та його назвою:**

*Оператори:*
1. `&`
2. `|`
3. `-`
4. `^`

*Назви операцій:*
* А) Об'єднання (Union)
* Б) Симетрична різниця (Symmetric Difference)
* В) Перетин (Intersection)
* Г) Відносна різниця (Difference)

---

#### РОЗДІЛ 6. АСИМПТОТИЧНА СКЛАДНІСТЬ, ПАМ'ЯТЬ ТА ГРАНИЧНІ СТАНИ

**51. Якою є середня часова складність перевірки належності елемента `x in S` для списку `list` та множини `set` відповідно?**
* А) $O(1)$ для списку та $O(N)$ для множини
* Б) $O(N)$ для списку та $O(1)$ для множини
* В) $O(\log N)$ для списку та $O(1)$ для множини
* Г) $O(1)$ для обох структур

**52. Якою є найгірша теоретична часова складність пошуку ключа у словнику `dict` у разі виникнення 100% колізій хешування?**
* А) $O(1)$
* Б) $O(\log N)$
* В) $O(N)$
* Г) $O(N^2)$

**53. Скільки кошиків у середньому перевіряє інтерпретатор CPython при пошуку елемента у хеш-таблиці з фактором заповнення до 66% згідно з моделлю Кнута?**
* А) Менше 3 перевірок
* Б) Рівно 10 перевірок
* В) Близько $\log_2(N)$ перевірок
* Г) Кількість перевірок дорівнює половині розміру таблиці ($N/2$)

**54. Що відбудеться з продуктивністю пошуку в множині `set`, якщо для всіх доданих користувацьких об'єктів метод `__hash__` повертатиме константу `42`?**
* А) Програма негайно збудить виняток `HashCollisionError`
* Б) Швидкість пошуку та вставки впаде від константної $O(1)$ до лінійної $O(N)$
* В) У множині зможе зберегтися лише один об'єкт, інші будуть відкинуті
* Г) Множина автоматично конвертується у незмінний кортеж

**55. Чому зберігання 1 мільйона цілих чисел у множині `set` потребує суттєво більше оперативної пам'яті, ніж у масиві `array.array('i')`?**
* А) `set` зберігає розріджену таблицю, покажчики на об'єкти та 64-бітні хеші, а `array` зберігає неперервні машинні байти
* Б) `set` автоматично дублює кожен елемент у незалежному файлі підкачки
* В) `array` стискає всі числа за алгоритмом LZW
* Г) `set` резервує 128 байтів під коментарі розробника для кожного кошика

**56. Чому операція додавання елемента у словник `dict` має амортизовану складність $O(1)$, а не строгу константну?**
* А) Швидкість залежить від завантаження шини пам'яті операційної системи
* Б) Періодичне розширення таблиці вимагає виділення нового РОЗДІЛу пам'яті та рехешування всіх елементів за час $O(N)$
* В) Інтерпретатор випадковим чином призупиняє виконання для виклику `time.sleep()`
* Г) Вставка елемента викликає лінійний зсув усіх сусідніх покажчиків праворуч

**57. Яку структуру даних мови Python слід обрати для збереження 1 000 000 заРОЗДІЛованих IP-адрес для забезпечення максимальної пропускної здатності фільтрації трафіку?**
* А) `list`
* Б) `tuple`
* В) `set`
* Г) `collections.deque`

**58. Яку ймовірнісну структуру даних доцільно використати замість великої множини `set`, якщо пам'ять строго обмежена кількома мегабайтами і допускається невеликий відсоток хибнопозитивних спрацьовувань?**
* А) Префіксне дерево (Trie)
* Б) Фільтр Блума (Bloom Filter)
* В) Черга з пріоритетом (Priority Queue)
* Г) Кільцевий буфер (Ring Buffer)

**59. У чому полягає перевага використання оператора `A |= B` над виразом `A = A | B` при обробці великих множин?**
* А) Працює в окремому потоці виконання без РОЗДІЛування GIL
* Б) Автоматично сортує елементи множини перед об'єднанням
* В) Модифікує множину `A` на місці, уникаючи виділення пам'яті під новий об'єкт та знижуючи навантаження на Garbage Collector
* Г) Дозволяє додавати до множини списки без генерації винятків

**60. Якщо для двох об'єктів `hash(a) == hash(b)`, це однозначно гарантує, що вираз `a == b` поверне `True`.**
* А) Правда (True)
* Б) Неправда (False)

## Тема 6

### РОЗДІЛ 1. АРХІТЕКТУРА ФУНКЦІЙ, PYFUNCTIONOBJECT ТА PYCODEOBJECT

**1. Що відбувається в середовищі CPython під час виконання рядка з інструкцією `def`?**  
а) Статично резервується незмінна адреса в сегменті машинних інструкцій  
б) Динамічно створюється об'єкт `PyFunctionObject` у купі та зв'язується з іменем у поточному просторі  
в) Вихідний код функції компілюється безпосередньо у машинний код цільового процесора  
г) Створюється новий стековий кадр `PyFrameObject` і призупиняється виконання  

**2. У чому полягає принципова відмінність між об'єктами `PyCodeObject` та `PyFunctionObject`?**  
а) `PyCodeObject` створюється в runtime, а `PyFunctionObject` генерується на етапі парсингу  
б) `PyFunctionObject` містить виключно байт-код, а `PyCodeObject` зберігає значення аргументів  
в) `PyCodeObject` є статичним незмінним байт-кодом, а `PyFunctionObject` — динамічною обгорткою з контекстом середовища  
г) Між ними немає різниці, це два синонімічні позначення одного C-типу  

**3. Які з наведених атрибутів належать до статичного об'єкта коду `PyCodeObject`?** *(Оберіть усі правильні відповіді)*  
а) `co_code` (масив опкодів байт-коду)  
б) `__globals__` (словник глобального простору модуля)  
в) `co_varnames` (кортеж імен локальних змінних)  
г) `__defaults__` (кортеж аргументів за замовчуванням)  
д) `co_consts` (пул констант та літералів)  

**4. Об'єкт байт-коду `PyCodeObject` є незмінним (immutable), тоді як екземпляр функції `PyFunctionObject` є змінюваним об'єктом у купі.**  
а) Істина (True)  
б) Хибність (False)  

**5. Яка інструкція байт-коду CPython створює об'єкт функції `PyFunctionObject` на основі об'єкта коду під час виконання інструкції `def`?**  
а) `BUILD_FUNCTION`  
б) `CALL_FUNCTION`  
в) `MAKE_FUNCTION`  
г) `LOAD_CODE`  

**6. Встановіть відповідність між атрибутом функції/коду та його системним вмістом:**  

| Атрибут | Системний вміст |
| :--- | :--- |
| **1.** `__globals__` | **А.** Сирий масив інструкцій байт-коду |
| **2.** `co_varnames` | **Б.** Кортеж попередньо обчислених позиційних значень за замовчуванням |
| **3.** `co_code` | **В.** Покажчик на простір імен глобального модуля |
| **4.** `__defaults__` | **Г.** Кортеж імен локальних змінних і параметрів |

**7. Що зберігає поле `co_varnames` об'єкта коду `PyCodeObject`?**  
а) Словник глобальних змінних модуля  
б) Кортеж імен локальних змінних та формальних параметрів функції  
в) Список імен усіх зовнішніх функцій, що викликаються  
г) Таблицю типів повернених значень  

**8. У мові Python інструкція `def` є суто статичною компіляторною директивою і не виконується як інструкція під час роботи програми.**  
а) Істина (True)  
б) Хибність (False)  

**9. Як називається низькорівнева C-структура в CPython, що інкапсулює скомпільовані інструкції байт-коду, пул констант і локальних імен підпрограми?**  
*(Впишіть назву структури)*: ____________________

**10. Яку інформацію зберігає цілочисельний атрибут `co_argcount` об'єкта коду `PyCodeObject`?**  
а) Загальну кількість усіх викликів функції за час роботи програми  
б) Кількість рядків вихідного коду в тілі функції  
в) Кількість фіксованих позиційних параметрів функції  
г) Глибину вкладеності рекурсивних викликів  

**11. Які поля зберігаються безпосередньо в екземплярі `PyFunctionObject` у купі пам'яті?** *(Оберіть усі правильні відповіді)*  
а) `co_stacksize`  
б) `__globals__`  
в) `__code__`  
г) `co_lnotab`  
д) `__annotations__`  

**12. Що відбудеться, якщо під час виконання програми динамічно замінити об'єкт коду функції `func.__code__` на інший сумісний `PyCodeObject`?**  
а) Інтерпретатор негайно збудить виняток `TypeError`  
б) Функція виконуватиме новий байт-код, зберігши власне ім'я та глобальний контекст модуля  
в) Зруйнується простір імен глобального модуля  
г) Зміна буде проігнорована, оскільки посилання кешуються назавжди  

---

### РОЗДІЛ 2. МОДЕЛЬ ПЕРЕДАЧІ ПАРАМЕТРІВ ТА ПОВЕРНЕННЯ ЗНАЧЕНЬ

**13. Яка модель передачі параметрів реалізована у мові Python?**  
а) Сувора передача за значенням з фізичним копіюванням байтів (*pass-by-value*)  
б) Передача за посиланням на спільний об'єкт (*pass-by-object-reference*)  
в) Сувора передача за посиланням з псевдонімами змінних (*pass-by-reference*)  
г) Передача за іменем через макропідстановку (*pass-by-name*)  

**14. Як поводиться функція при спробі модифікації незмінюваного аргументу (наприклад, `int` або `str`) через оператор присвоєння всередині свого тіла?**  
а) Модифікує пам'ять вихідного об'єкта безпосередньо у викликаючому модулі  
б) Збуджує виняток `ImmutableMutationError` під час виконання  
в) Створює новий незалежний об'єкт у купі та перенаправляє локальне ім'я, не змінюючи значення в викликаючому коді  
г) Автоматично перетворює змінну на тип `list`  

**15. Що відбувається при виклику методу `append()` над переданим у функцію аргументом типу `list`?**  
а) Створюється локальна копія списку, тому викликаючий код не бачить змін  
б) Змінюється стан того самого об'єкта в купі, що призводить до побічного ефекту у викликаючому середовищі  
в) Збуджується виняток `TypeError` через порушення чистоти функції  
г) Список автоматично конвертується у незмінний кортеж  

**16. Під час передачі списку `list` як аргументу у функцію CPython завжди створює його повну копію для ізоляції даних.**  
а) Істина (True)  
б) Хибність (False)  

**17. Як змінюється лічильник посилань `ob_refcnt` об'єкта під час його передачі як аргументу у виклик функції на час існування стекового кадру?**  
а) Залишається незмінним, оскільки аргументи не враховуються збирачем сміття  
б) Зменшується на одиницю, оскільки право власності переходить функції  
в) Збільшується щонайменше на одиницю через появу локального посилання в `PyFrameObject`  
г) Скидається до нуля до моменту завершення функції  

**18. Яке значення повертає функція, у тілі якої відсутній явний оператор `return`?**  
а) `0`  
б) `False`  
в) `None`  
г) Порожній рядок `""`  

**19. Усі функції, які не повертають явного значення, повертають посилання на один і той самий глобальний об'єкт-сінглтон `_Py_NoneStruct` (`id(x) == id(None)`).**  
а) Істина (True)  
б) Хибність (False)  

**20. Яка інструкція байт-коду генерується компілятором Python для упаковки декількох значень у виразі `return a, b, c`?**  
а) `PACK_SEQUENCE`  
б) `BUILD_TUPLE`  
в) `BUILD_LIST`  
г) `MAKE_COLLECTION`  

**21. Який тип даних автоматично формує Python для повернення кількох значень, якщо вони перелічені через кому у виразі `return x, y, z`?**  
*(Впишіть назву типу)*: ____________________

**22. Який опкод байт-коду CPython відповідає за структурне розпакування повернутого кортежу у виразі `x, y = get_data()`?**  
а) `POP_TOP`  
б) `STORE_FAST_MULTIPLE`  
в) `UNPACK_TUPLE`  
г) `UNPACK_SEQUENCE`  

**23. Які з наведених типів даних є незмінюваними (immutable) і гарантують збереження свого значення у викликаючому коді при передачі у функцію?** *(Оберіть усі правильні відповіді)*  
а) `int`  
б) `list`  
в) `str`  
г) `dict`  
д) `tuple`  

**24. Що відбувається з локальними змінними стекового кадру `PyFrameObject` після виконання термінальної інструкції `RETURN_VALUE`?**  
а) Декрементуються лічильники посилань об'єктів, а кадр звільняється менеджером пам'яті  
б) Об'єкти локальних змінних автоматично записуються у глобальний простір  
в) Пам'ять кадру копіюється в стек виклику наступної функції  
г) Локальні змінні залишаються у пам'яті до повного завершення роботи програми  

---

### РОЗДІЛ 3. ФУНКЦІЇ ЯК ОБ'ЄКТИ ПЕРШОГО КЛАСУ ТА ІНТРОСПЕКЦІЯ

**25. Що означає концепція «функція як об'єкт першого класу» (First-Class Citizen) у мові Python?**  
а) Функції мають найвищий пріоритет обчислень під час роботи планувальника ОС  
б) Функції можуть динамічно створюватися, передаватися як аргументи, повертатися з підпрограм та зберігатися в колекціях  
в) Функції повинні обов'язково бути задекларовані на початку файлу модуля  
г) Функції не можуть бути вкладеними в інші підпрограми  

**26. Завдяки заповненню якого системного C-слота в структурі `PyTypeObject` екземпляр функції або класу стає викликовою сутністю (callable)?**  
а) `tp_init`  
б) `tp_call`  
в) `tp_new`  
г) `tp_descr_get`  

**27. Які операції дозволено виконувати з функціями у мові Python завдяки їхньому статусу First-Class Citizens?** *(Оберіть усі правильні відповіді)*  
а) Передавати як аргументи іншим функціям  
б) Безпосередньо змінювати машинний бінарний код без участі PVM  
в) Повертати як результат виконання підпрограм  
г) Викликати без використання круглих дужок у довільних місцях  
д) Зберігати як значення у словниках або елементи списків  

**28. Яку архітектурну структуру можна побудувати за допомогою функцій першого класу для заміни довгих каскадів `if-elif-else`?**  
а) Бінарне дерево розбору граматики  
б) Стек рекурсивних кадрів  
в) Таблицю диспетчеризації (*dispatch table*) на основі словника  
г) Чергу повідомлень FIFO на диску  

**29. Встановіть відповідність між службовим атрибутом функції та інформацією, яку він зберігає:**  

| Атрибут | Інформація |
| :--- | :--- |
| **1.** `__doc__` | **А.** Кортеж комірок `PyCellObject` із захопленими змінними |
| **2.** `__name__` | **Б.** Рядок документації функції (*docstring*) |
| **3.** `__annotations__` | **В.** Офіційний строковий ідентифікатор назви підпрограми |
| **4.** `__closure__` | **Г.** Словник анотацій типів параметрів та результату |

**30. У мові Python функції можна використовувати як значення у словниках для організації швидкої маршрутизації команд за константний час $O(1)$.**  
а) Істина (True)  
б) Хибність (False)  

**31. Який стандартний модуль Python використовується для дизасемблювання та інспекції інструкцій байт-коду об'єкта коду функції?**  
а) `sys`  
б) `dis`  
в) `gc`  
г) `ctypes`  

**32. У якій структурі даних зберігаються оголошені типи параметрів функції всередині атрибута `__annotations__`?**  
а) Словник (`dict`)  
б) Кортеж (`tuple`)  
в) Список (`list`)  
г) Незмінна множина (`frozenset`)  

---

### РОЗДІЛ 4. РОЗШИРЕНЕ ЗВ'ЯЗУВАННЯ, СИНТАКСИС PEP 570 ТА PEP 3102

**33. У яку структуру даних пакуються всі надлишкові позиційні аргументи при використанні конструкції `*args` у заголовку функції?**  
а) Список (`list`)  
б) Словник (`dict`)  
в) Множина (`set`)  
г) Кортеж (`tuple`)  

**34. У яку структуру даних пакуються непередбачені іменовані аргументи при використанні конструкції `**kwargs` у сигнатурі функції?**  
а) Кортеж (`tuple`)  
б) Словник (`dict`)  
в) Список (`list`)  
г) Незмінна множина (`frozenset`)  

**35. Який символ у сигнатурі функції відповідно до специфікації PEP 570 позначає межу виключно позиційних параметрів (positional-only)?**  
а) `*`  
б) `/`  
в) `&`  
г) `$`  

**36. Який символ відокремлює групу виключно іменованих параметрів (keyword-only) згідно з PEP 3102?**  
а) `/`  
б) `#`  
в) `*`  
г) `@`  

**37. Задано функцію: `def configure(ip, port, /, timeout=10): pass`. Який із наведених викликів збудить виняток `TypeError`?**  
а) `configure("127.0.0.1", 8080)`  
б) `configure(ip="127.0.0.1", port=8080)`  
в) `configure("127.0.0.1", 8080, 5)`  
г) `configure("127.0.0.1", 8080, timeout=5)`  

**38. Який правильний порядок розміщення категорій параметрів встановлено специфікацією мови Python?** *(Оберіть усі правильні твердження)*  
а) Параметри positional-only розташовуються суворо ліворуч від роздільника `/`  
б) Словник `**kwargs` може розташовуватися перед позиційними параметрами  
в) Параметри keyword-only розташовуються строго праворуч від одиночної зірочки `*`  
г) Роздільник `/` може знаходитися після параметрів `*args`  

**39. Параметри функції, задекларовані ліворуч від роздільника `/`, дозволяється передавати як іменовані аргументи (наприклад, `func(param=value)`).**  
а) Істина (True)  
б) Хибність (False)  

**40. Параметри функції, розташовані праворуч від одиночної зірочки `*`, дозволяється передавати позиційно без вказівки їхнього імені.**  
а) Істина (True)  
б) Хибність (False)  

**41. За допомогою якого префіксного оператора виконується структурне розпакування списку у позиційні аргументи виклику функції `func(*data)`?**  
а) `**`  
б) `*`  
в) `&`  
г) `%`  

**42. Який оператор використовується для розпакування пар ключ-значення словника в іменовані аргументи виклику `func(**options)`?**  
а) `*`  
б) `$$`  
в) `**`  
г) `##`  

---

### РОЗДІЛ 5. ОБЛАСТІ ВИДИМОСТІ LEGB, ЗАМИКАННЯ ТА ДИРЕКТИВИ

**43. У якій послідовності інтерпретатор Python здійснює пошук ідентифікаторів змінних згідно з правилом LEGB?**  
а) Global $\to$ Local $\to$ Enclosing $\to$ Built-in  
б) Local $\to$ Enclosing $\to$ Global $\to$ Built-in  
в) Built-in $\to$ Global $\to$ Enclosing $\to$ Local  
г) Local $\to$ Global $\to$ Enclosing $\to$ Built-in  

**44. Введіть 4-літерну абревіатуру ієрархічного правила дозволу просторів імен у мові Python.**  
*(Впишіть відповідь)*: ____________________

**45. Що являє собою функціональне замикання (closure) у мові Python?**  
а) Підпрограму, яка не має оператора `return`  
б) Рекурсивний виклик функції самої себе до досягнення базового випадку  
в) Вкладену функцію, яка утримує посилання на змінні з охоплюючого лексичного простору навіть після його завершення  
г) Функцію, в якій заборонено використання глобальних змінних  

**46. Який низькорівневий об'єкт у купі пам'яті CPython забезпечує збереження та спільний доступ до захоплених змінних у замиканнях?**  
а) `PyFrameObject`  
б) `PyCellObject`  
в) `PyStackObject`  
г) `PyScopeObject`  

**47. Для чого використовується директива `nonlocal` всередині вкладеної функції?**  
а) Для оголошення наміру перезаписати зв'язування змінної у найближчому зовнішньому просторі Enclosing  
б) Для створення нової глобальної змінної на рівні модуля  
в) Для імпорту змінних із вбудованого простору Built-in  
г) Для захисту змінної від збирання сміття  

**48. Яке призначення має інструкція `global x` всередині локального тіла підпрограми?**  
а) Створює копію локальної змінної для використання в інших потоках  
б) Забороняє видалення змінної `x` функцією `del`  
в) Вказує компілятору пов'язувати всі операції з ідентифікатором `x` безпосередньо з простором імен модуля  
г) Робить функцію доступною для виклику з інших файлів проекту  

**49. За яких обставин виникає виняток `UnboundLocalError: local variable referenced before assignment`?**  
а) Функція викликається з меншою кількістю аргументів, ніж визначено в сигнатурі  
б) Змінна модифікується в тілі функції (присвоєння), але зчитується раніше за це присвоєння без оголошення `global`/`nonlocal`  
в) Спроба звернення до глобальної змінної, яка містить значення `None`  
г) Використання рекурсії з перевищенням ліміту 1000 викликів  

**50. Встановіть відповідність між рівнем видимості LEGB та джерелом його формування:**  

| Рівень LEGB | Джерело формування |
| :--- | :--- |
| **1.** Local | **А.** Системний простір вбудованих функцій, типів та винятків |
| **2.** Enclosing | **Б.** Простір імен поточного завантаженого файлу (модуля) |
| **3.** Global | **В.** Аргументи та змінні, зв'язані всередині поточного кадру функції |
| **4.** Built-in | **Г.** Локальні простори імен охоплюючих (зовнішніх) функцій |

---

### РОЗДІЛ 6. LAMBDA, ПАСТКИ ПАМ'ЯТІ ТА БАР'ЄРИ РЕКУРСІЇ

**51. Яке фундаментальне синтаксичне обмеження накладає мова Python на анонімні `lambda`-функції?**  
а) В анонімних функціях заборонено використання параметрів  
б) `Lambda`-функції не можуть повертати числові значення  
в) Анонімні функції можуть викликатися лише один раз за час роботи програми  
г) Тіло може містити виключно один обчислюваний вираз без керуючих інструкцій (`for`, `while`, `try`)  

**52. Що повертає вбудована функція вищого порядку `map(func, iterable)` у мові Python 3?**  
а) Повнорозмірний список (`list`) із усіма обчисленими значеннями  
б) Кортеж незмінних елементів (`tuple`)  
в) Лінивий ітератор, елементи якого обчислюються по одному за запитом  
г) Об'єкт генератора списку (*list comprehension*)  

**53. Який об'єкт повертає функція `filter(predicate, sequence)` у Python 3?**  
а) Новий словник з відфільтрованими ключами  
б) Лінивий ітератор, що пропускає елементи, для яких предикат повернув `True`  
в) Незмінну множину `frozenset`  
г) Повністю згенерований список у пам'яті  

**54. Коли саме обчислюється вираз значення аргументу за замовчуванням у заголовку `def func(data=[]):`?**  
а) Заново перед кожним окремим викликом функції  
б) Один раз під час завантаження модуля та первинного виконання інструкції `def`  
в) Лише тоді, коли аргумент явно не передано при виклику  
г) Під час першого виклику збирача сміття `gc.collect()`  

**55. Що виведе на екран наступний фрагмент коду?**
```python
def append_val(x, items=[]):
    items.append(x)
    return len(items)

print(append_val(1), append_val(2))
```
а) `1 1`  
б) `2 2`  
в) `1 2`  
г) Виникне виняток `TypeError`  

**56. Віртуальна машина CPython автоматично оптимізує хвостову рекурсію (TCO), запобігаючи зростанню стека викликів.**  
а) Істина (True)  
б) Хибність (False)  

**57. Який виняток виникає у CPython, якщо глибина рекурсивних викликів перевищує встановлений системний ліміт?**  
а) `StackOverflowError`  
б) `MemoryError`  
в) `RecursionError`  
г) `SystemError`  

**58. Яке значення виведе вираз `handlers[0](10)` для списку замикань: `handlers = [lambda x: x * i for i in range(5)]`?**  
а) `0`  
б) `40`  
в) `10`  
г) `50`  

**59. Якими методами можна усунути ефект небажаного пізнього зв'язування у замиканнях усередині циклів?** *(Оберіть усі правильні відповіді)*  
а) Фіксацією поточного стану через аргумент за замовчуванням: `lambda x, i=i: x * i`  
б) Використанням інструкції `global` всередині тіла `lambda`  
в) Використанням фабричної функції `functools.partial`  
г) Примусовим збільшенням ліміту рекурсії `sys.setrecursionlimit()`  

**60. Яка функція стандартного модуля `sys` повертає поточний системний ліміт глибини рекурсії інтерпретатора Python?**  
*(Впишіть назву функції)*: ____________________

## Тема 7

### РОЗДІЛ 1. МОДУЛІ В CPYTHON, РЕЄСТР SYS.MODULES ТА КОНВЕЄР ІМПОРТУ

---

**1. Яка C-структура інкапсулює завантажений модуль як об'єкт першого класу в середовищі CPython?**
* А) PyModuleObject
* Б) PyCodeObject
* В) PyPackageObject
* Г) PyNamespaceObject

---

**2. Яка структура даних мови Python забезпечує ізоляцію простору імен модуля і доступна через службовий атрибут `__dict__`?**
* А) dict
* Б) list
* В) set
* Г) tuple

---

**3. Що виконує підсистема імпорту CPython на найпершому етапі конвеєра завантаження модуля?**
* А) Перевіряє наявність модуля у глобальному кеші `sys.modules`
* Б) Сканує каталоги файлової системи за списком `sys.path`
* В) Компілює вихідний код у синтаксичне дерево AST
* Г) Створює порожній каталог `__pycache__`

---

**4. Якщо ім'я запитуваного модуля вже присутнє у словнику `sys.modules`, інтерпретатор повторно виконує його вихідний код для оновлення стану.**
* А) Істина (True)
* Б) Хибність (False)

---

**5. Які стандартні класи-виявлячі (finders) послідовно опитуються у списку `sys.meta_path` під час пошуку модуля? *(Оберіть усі правильні відповіді)***
* А) BuiltinImporter (для вбудованих модулів ядра)
* Б) FrozenImporter (для скомпільованих frozen-модулів)
* В) PathFinder (для пошуку файлів у каталогах `sys.path`)
* Г) ThreadImporter (для паралельних потоків)
* Д) SocketImporter (для завантаження через мережеві сокети)

---

**6. Який об'єкт формується виявлячем (finder) і містить метадані про походження модуля та посилання на відповідний завантажувач (loader)?**
* А) ModuleSpec
* Б) PyCodeObject
* В) NamespacePath
* Г) ImportRegistry

---

**7. За якою умовою скомпільований файл байт-коду `.pyc` у директорії `__pycache__` вважається актуальним?**
* А) Часова мітка модифікації або хеш вихідного `.py` збігається з метаданими заголовка `.pyc`
* Б) Розмір файлу `.pyc` строго більший за 1024 байти
* В) Файл `.pyc` створено не раніше ніж за 24 години до поточного запуску
* Г) Контрольна сума CRC32 файлу `.pyc` дорівнює нулю

---

**8. У якій спеціальній піддиректорії інтерпретатор CPython зберігає скомпільований байт-код `.pyc` для оптимізації повторного завантаження?**
* А) `__pycache__`
* Б) `__bytecode__`
* В) `.sys_cache`
* Г) `.python_cache`

---

**9. Бінарні динамічні бібліотеки мов C/C++ із розширеннями `.so` (POSIX) або `.pyd` (Windows) є повноцінними модулями в архітектурі CPython.**
* А) Істина (True)
* Б) Хибність (False)

---

**10. Вкажіть ім'я глобального асоціативного словника інтерпретатора, що виконує роль кешу вже завантажених екземплярів модулів.**
* Впишіть відповідь: `____________________`

---

**11. Встановіть відповідність між компонентом підсистеми імпорту та його системною роллю:**

**Компоненти:**
1. `sys.modules`
2. `sys.meta_path`
3. `ModuleSpec`
4. `__dict__`

**Системні ролі:**
* А) Внутрішній простір імен завантаженого модуля
* Б) Специфікація метаданих модуля для завантажувача
* В) Глобальний адресний кеш завантажених об'єктів модулів
* Г) Список пріоритетних класів-виявлячів (finders)

---

**12. Скільки разів виконується верхньорівневий виконуваний код модуля за замовчуванням протягом життєвого циклу процесу CPython?**
* А) Один раз під час первинного завантаження
* Б) Щоразу при кожному виклику інструкції import у будь-якому файлі
* В) Двічі: під час парсингу та під час фінальної реєстрації
* Г) Виконується лише при прямому запуску через термінал

---

**13. Який виняток генерує підсистема імпорту CPython, якщо модуль із вказаним ім'ям не вдалося знайти жодному зареєстрованому виявлячу?**
* А) ModuleNotFoundError
* Б) ImportWarning
* В) FileNotFoundError
* Г) AttributeError

---

**14. Які розширення файлів можуть фізично представляти імпортовані модулі у CPython на POSIX-сумісних системах? *(Оберіть усі правильні відповіді)***
* А) `.py` (вихідний текст скрипта)
* Б) `.pyc` (скомпільований байт-код)
* В) `.so` (динамічна бібліотека C/C++)
* Г) `.exe` (виконуваний файл Windows)
* Д) `.bin` (сирий бінарний дамп пам'яті)

---
---

### РОЗДІЛ 2. СИНТАКСИС ІМПОРТУВАННЯ ТА ПРОСТОРИ ІМЕН

---

**15. Як змінюється локальна таблиця символів при виконанні інструкції `import math`?**
* А) Додається єдиний ідентифікатор math, який посилається на об'єкт модуля
* Б) Усі функції та константи модуля math копіюються безпосередньо в локальний простір
* В) Локальний простір імен очищається перед імпортом
* Г) Створюється новий ізольований процес операційної системи

---

**16. У чому полягає перевага у швидкодії виклику `sin(x)` після `from math import sin` над `math.sin(x)` після `import math` усередині інтенсивного циклу?**
* А) Прямий виклик виконує інструкцію LOAD_GLOBAL або LOAD_FAST замість важчого пошуку атрибута LOAD_ATTR
* Б) Прямий імпорт автоматично компілює функцію sin в апаратні інструкції CPU
* В) При виклику через крапку інтерпретатор завжди заново відкриває файл math.py
* Г) Виклик sin(x) вимикає механізм GIL у віртуальній машині

---

**17. У модулі `data_hub.py` визначено `rate = 100`. Модуль `worker.py` виконав `from data_hub import rate`. Пізніше інший потік виконав `data_hub.rate = 200`. Яким буде значення змінної `rate` у `worker.py`?**
* А) 100, оскільки зв'язування відбулося за значенням посилання на незмінюваний об'єкт int
* Б) 200, оскільки імпорт автоматично створює динамічний двосторонній зв'язок
* В) None, оскільки виникає стан гонитви (race condition)
* Г) Виникне виняток UnboundLocalError

---

**18. Що імпортується у поточний простір імен при виконанні `from module import *`, якщо атрибут `__all__` у модулі не визначено?**
* А) Усі ідентифікатори модуля, крім тих, що починаються із символу нижнього підкреслення `_`
* Б) Абсолютно всі ідентифікатори модуля без винятків
* В) Тільки функції та класи, ігноруючи числові змінні
* Г) Жоден ідентифікатор не буде імпортовано, оскільки `__all__` є обов'язковим

---

**19. Що потрапить у локальну таблицю символів при виконанні `from module import *`, якщо в модулі визначено `__all__ = ['calc', 'DATA']`?**
* А) Виключно ідентифікатори 'calc' та 'DATA'
* Б) Усі публічні імена модуля разом із 'calc' та 'DATA'
* В) Усі приватні імена модуля, які починаються з `_`
* Г) Жодне ім'я, оскільки список `__all__` забороняє зірочковий імпорт

---

**20. Використання конструкції `import telemetry as tm` змінює значення внутрішнього службового атрибута `telemetry.__name__` на рядок `"tm"`.**
* А) Істина (True)
* Б) Хибність (False)

---

**21. Чому конструкція `from module import *` вважається антипатерном в інженерному програмуванні?**
* А) Вона неконтрольовано засмічує локальний простір імен і неявно затінює раніше визначені ідентифікатори
* Б) Вона збільшує споживання оперативної пам'яті комп'ютера у 10 разів
* В) Вона РОЗДІЛує можливість збирання сміття Garbage Collector
* Г) Вона заборонена стандартом PEP 8 і викликає синтаксичну помилку

---

**22. Встановіть відповідність між синтаксичною формою імпорту та її впливом на простір імен:**

**Форми імпорту:**
1. `import M`
2. `from M import f`
3. `import M as alias`
4. `from M import *`

**Вплив на простір імен:**
* А) Додає посилання на модуль M під локальним псевдонімом alias
* Б) Масово копіює публічні імена модуля згідно з контрактом `__all__`
* В) Додає ім'я модуля M; доступ до компонентів здійснюється через `M.attr`
* Г) Додає пряме посилання на f; виклик без префікса

---

**23. Інструкція `from package import subpackage` зв'язує ім'я `subpackage` безпосередньо в поточному просторі імен викликаючого модуля.**
* А) Істина (True)
* Б) Хибність (False)

---

**24. Які інструкції імпорту модифікують локальну таблицю символів викликаючого контексту? *(Оберіть усі правильні відповіді)***
* А) `import A`
* Б) `from A import B`
* В) `import A as C`
* Г) `pass`
* Д) `return`

---
---

### РОЗДІЛ 3. АТРИБУТ \_\_NAME\_\_ ТА ТОЧКА ВХОДУ

---

**25. Яке значення автоматично отримує системний атрибут `__name__` модуля, якщо його файл передано інтерпретатору як пряму точку входу процесу?**
* А) `"__main__"`
* Б) `"__entry__"`
* В) Ім'я файлу без розширення `.py`
* Г) `"root"`

---

**26. Яке значення отримує атрибут `__name__` файлу `sensor.py`, якщо він завантажується іншим модулем через команду `import sensor`?**
* А) `"sensor"`
* Б) `"__main__"`
* В) `"sensor.py"`
* Г) `"__imported__"`

---

**27. Яке головне архітектурне призначення канонічної ідіоми `if __name__ == '__main__':`?**
* А) Відокремлення виконуваного коду запуску та тестів від експортованих визначень функцій і класів
* Б) Запобігання витоку пам'яті при роботі з циклічними структурами
* В) Прискорення компіляції вихідного коду в інструкції байт-коду
* Г) Примусова реєстрація модуля у словнику `sys.modules`

---

**28. При запуску модуля як скрипта через утиліту `python -m sensor` значення атрибута `__name__` всередині `sensor.py` дорівнює `"sensor"`.**
* А) Істина (True)
* Б) Хибність (False)

---

**29. Чим відрізняється вміст нульового елемента `sys.path[0]` при запуску `python -m script` порівняно з `python path/to/script.py`?**
* А) Містить поточний робочий каталог термінала (CWD) замість каталогу розташування файлу script.py
* Б) Містить шлях до каталогу стандартної бібліотеки Python
* В) Завжди залишається порожнім рядком `""`
* Г) Вказує безпосередньо на каталог віртуального оточення site-packages

---

**30. Вкажіть точне значення рядкового літерала, яке отримує змінна `__name__` файлу при його прямому запуску інтерпретатором.**
* Впишіть відповідь: `____________________`

---
---

### РОЗДІЛ 4. ПАКЕТИ, \_\_INIT\_\_.PY, ПРОСТОРОВІ ПАКЕТИ ТА \_\_ALL\_\_

---

**31. Який обов'язковий файл повинен бути присутнім у директорії файлової системи, щоб інтерпретатор Python визнав її регулярним пакетом?**
* А) `__init__.py`
* Б) `__main__.py`
* В) `__package__.py`
* Г) `package.json`

---

**32. Коли саме виконується код, розміщений у файлі `__init__.py` регулярного пакета?**
* А) Один раз у момент першого імпортування самого пакета або будь-якого його підмодуля
* Б) Під час кожного звернення до будь-якої функції всередині пакета
* В) Виключно під час завершення роботи віртуальної машини Python
* Г) Лише якщо пакет викликається напряму через консольну утиліту

---

**33. Що являє собою просторовий пакет (Namespace Package) відповідно до стандарту PEP 420 (введеного у Python 3.3)?**
* А) Каталог без файлу `__init__.py`, компоненти якого можуть бути фізично розділені між різними директоріями чи архівами
* Б) Пакет, скомпільований у машинний код бібліотеки мови C
* В) Пакет, що зберігається виключно в оперативній пам'яті без створення файлів
* Г) Архів zip, який захищено криптографічним сертифікатом

---

**34. Для створення просторового пакета за специфікацією PEP 420 у кожній фізичній директорії необхідно створити порожній файл `__init__.py`.**
* А) Істина (True)
* Б) Хибність (False)

---

**35. Який інженерний патерн реалізує файл `__init__.py`, коли імпортує внутрішні класи підмодулів для надання користувачу простого зовнішнього інтерфейсу?**
* А) Фасад (Facade)
* Б) Одинак (Singleton)
* В) Адаптер (Adapter)
* Г) Спостерігач (Observer)

---

**36. До якого типу даних належить спеціальний атрибут `__all__`, що визначає публічний інтерфейс експорту модуля?**
* А) Список рядків (list) або кортеж рядків (tuple)
* Б) Словник функцій (dict)
* В) Множина цілих чисел (set)
* Г) Булевий прапорець (bool)

---

**37. Якщо функція `helper()` не вказана у списку `__all__` модуля `core`, це повністю РОЗДІЛує можливість її виклику через прямий імпорт `import core; core.helper()`.**
* А) Істина (True)
* Б) Хибність (False)

---

**38. Які інженерні переваги забезпечує явна декларація списку `__all__` у модулі? *(Оберіть усі правильні відповіді)***
* А) Регламентує перелік імен для експорту конструкцією `from module import *`
* Б) Слугує формальною угодою публічного контракту для лінтерів і генераторів документації
* В) Шифрує внутрішній байт-код модуля від декомпіляції
* Г) Забороняє імпортувати модуль стороннім скриптам

---

**39. Починаючи з Python 3.3, будь-яку директорію з файлами `.py` можна імпортувати як просторовий пакет без наявності в ній файлу `__init__.py`.**
* А) Істина (True)
* Б) Хибність (False)

---

**40. Встановіть відповідність між архітектурним поняттям та його описом:**

**Поняття:**
1. Регулярний пакет
2. Просторовий пакет
3. Список `__all__`
4. Файл `__init__.py`

**Описи:**
* А) Регламентований перелік публічно експортованих імен
* Б) Каталог із обов'язковим файлом `__init__.py`
* В) Точка входу та фасадний ініціалізатор пакета
* Г) Децентралізований пакет без `__init__.py` (PEP 420)

---
---

### РОЗДІЛ 5. SYS.PATH, ЦИКЛІЧНИЙ ІМПОРТ ТА ЗАТІНЕННЯ МОДУЛІВ

---

**41. Що містить нульовий елемент `sys.path[0]` при стандартному запуску програми через `python app/main.py`?**
* А) Абсолютний шлях до каталогу, де безпосередньо лежить файл `main.py`
* Б) Шлях до каталогу встановлення виконуваного файлу python
* В) Порожній рядок для позначення стандартної бібліотеки
* Г) Шлях до каталогу віртуального середовища site-packages

---

**42. Яка системна змінна середовища операційної системи дозволяє користувачеві додати власні директорії до списку `sys.path` до запуску Python?**
* А) PYTHONPATH
* Б) PYTHONSYS
* В) PATH_MODULES
* Г) PYTHON_INCLUDE

---

**43. Чому інструкція `sys.path.insert(0, custom_path)` несе потенційний ризик у виробничих системах?**
* А) Вона надає вказаному каталогу найвищий пріоритет пошуку, що може затінити стандартні та системні модулі
* Б) Вона РОЗДІЛує доступ до мережевих сокетів операційної системи
* В) Вона автоматично очищає словник кешу `sys.modules`
* Г) Вона вимагає обов'язкового запуску інтерпретатора з правами root/Administrator

---

**44. Яка головна причина генерації винятку `ImportError: cannot import name ... from partially initialized module`?**
* А) Взаємна циклічна залежність модулів при верхньорівневому селективному імпорті неініціалізованих атрибутів
* Б) Помилка виділення динамічної пам'яті під об'єкт PyModuleObject
* В) Збіг контрольних сум двох однакових за змістом файлів
* Г) Спроба імпортувати модуль, який не містить списку `__all__`

---

**45. Які інженерні підходи дозволяють локалізувати або усунути циклічний імпорт? *(Оберіть усі правильні відповіді)***
* А) Винесення спільних типів даних та інтерфейсів у третій спільний модуль нижчого рівня
* Б) Використання відкладеного (deferred) імпорту всередині тіла функції
* В) Перехід від `from module import attr` до чистого `import module`
* Г) Перейменування обох модулів на ім'я `__init__.py`
* Д) Встановлення змішаних відступів табуляцією у вихідному коді

---

**46. У чому полягає архітектурний дефект «затінення модуля» (Module Shadowing)?**
* А) Створення користувацького файлу з ім'ям стандартного модуля (наприклад, `math.py`), що перекриває системну бібліотеку через `sys.path[0]`
* Б) Випадкове видалення файлу `__init__.py` із кореневого пакета
* В) Приховування вихідного коду за допомогою утиліт компіляції Cython
* Г) Використання однакових імен змінних усередині вкладених функцій

---

**47. Імпорт `import MyModule`, що успішно працював на машині розробника з Windows (NTFS), може аварійно завершитися `ModuleNotFoundError` на сервері Linux (ext4).**
* А) Істина (True)
* Б) Хибність (False)

---

**48. Як поводиться CPython, якщо файлова система змонтована в режимі «тільки для читання» (read-only) і запис у `__pycache__` неможливий?**
* А) Скрипт успішно виконується, компілюючи байт-код у пам'яті без збереження на диск, але час старту процесу зростає
* Б) Інтерпретатор аварійно зупиняє роботу із системним винятком PermissionError
* В) Автоматично відключається перевірка синтаксичних помилок у коді
* Г) Усі інструкції імпорту замінюються на виклики функції pass

---

**49. Як називається список у модулі `sys`, елементи якого задають черговість каталогів для пошуку імпортованих модулів?**
* Впишіть відповідь: `____________________`

---

**50. Які компоненти входять до складу сформованого списку `sys.path` під час старту інтерпретатора? *(Оберіть усі правильні відповіді)***
* А) Директорія запущеного скрипта (`sys.path[0]`)
* Б) Шляхи, вказані у системній змінній середовища PYTHONPATH
* В) Каталоги стандартної бібліотеки та каталог сторонніх пакунків site-packages
* Г) Таблиця кешу ядра `sys.modules`
* Д) Таблиця відкритих мережевих сокетів операційної системи

---
---

### РОЗДІЛ 6. ВЗАЄМОДІЯ З ОТОЧЕННЯМ: SYS, OS, PATHLIB, RANDOM, TIME

---

**51. Що завжди містить нульовий елемент списку аргументів командного рядка `sys.argv[0]`?**
* А) Шлях або ім'я запущеного файлу скрипта
* Б) Кількість переданих системних параметрів
* В) Повний список активних змінних середовища
* Г) Версію операційної системи

---

**52. Яка функція модуля `sys` повертає фізичний розмір об'єкта в оперативній пам'яті у байтах на рівні структур CPython?**
* А) `sys.getsizeof()`
* Б) `sys.memory_footprint()`
* В) `sys.len()`
* Г) `sys.size()`

---

**53. Який зі стандартних системних потоків дескрипторів модуля `sys` за замовчуванням є небуферизованим для негайного виведення діагностики збоїв?**
* А) `sys.stderr`
* Б) `sys.stdout`
* В) `sys.stdin`
* Г) `sys.stdlog`

---

**54. Який математичний бінарний оператор перевантажено в класі `pathlib.Path` для кросплатформного об'єднання шляхів файлової системи?**
* А) `/`
* Б) `+`
* В) `\`
* Г) `%`

---

**55. Чому функція `time.perf_counter()` є кращою за `time.time()` для прецизійного вимірювання тривалості інженерних операцій?**
* А) Вона є монотонним апаратним таймером високої точності і не зазнає стрибків при системному коригуванні часу за NTP
* Б) Вона повертає результат у форматі рядка ISO 8601
* В) Вона вимірює виключно астрономічний час доби від початку епохи UNIX
* Г) Вона автоматично зупиняє фонові потоки для підвищення швидкодії

---

**56. Функція `time.time()` є монотонним таймером і гарантує, що наступне значення часу ніколи не буде меншим за попереднє.**
* А) Істина (True)
* Б) Хибність (False)

---

**57. Який математичний алгоритм генерації псевдовипадкових чисел покладено в основу стандартного модуля `random`?**
* А) Вихор Мерсенна (Mersenne Twister MT19937)
* Б) Лінійний конгруентний генератор (LCG)
* В) Криптографічний алгоритм RSA
* Г) Генератор Фібоначчі з запізнюванням

---

**58. Чому алгоритм генератора `random` (MT19937) категорично заборонено використовувати для генерації ключів шифрування та токенів автентифікації?**
* А) Він не є криптографічно стійким, і його стан повністю відновлюється за 624 послідовними числами
* Б) Він підтримує генерацію чисел виключно в діапазоні від 0 до 100
* В) Він працює повільніше за системний генератор ядра операційної системи
* Г) Він не дозволяє фіксувати початковий стан генератора через seed

---

**59. Який модуль стандартної бібліотеки Python слід використовувати для генерації криптографічно стійких випадкових чисел і токенів безпеки?**
* Впишіть відповідь: `____________________`

---

**60. Встановіть відповідність між модулем стандартної бібліотеки та його цільовим інженерним призначенням:**

**Модулі:**
1. `sys`
2. `os`
3. `pathlib`
4. `time`

**Призначення:**
* А) Об'єктно-орієнтована кросплатформна робота зі шляхами файлової системи
* Б) Керування станом інтерпретатора CPython, аргументами CLI та потоками I/O
* В) Робота з системним часом, затримками sleep та монотонний бенчмаркінг
* Г) Пряма взаємодія з ядром операційної системи та змінними середовища

## Тема 8

### РОЗДІЛ 1. СИНТАКСИЧНІ VS ДИНАМІЧНІ ПОМИЛКИ, РОЗКРУЧУВАННЯ СТЕКА

**1. На якому етапі функціонування інтерпретатора CPython виникають синтаксичні помилки (SyntaxError, IndentationError)?**
* а) На етапі статичного синтаксичного аналізу до початку виконання байт-коду PVM
* б) Безпосередньо під час інтерпретації інструкцій віртуальною машиною
* в) Під час роботи автоматичного збирача сміття Garbage Collector
* г) Під час вивантаження модуля з пам'яті операційною системою

**2. Чи може РОЗДІЛ try-except перехопити синтаксичну помилку SyntaxError, що виникла всередині захищеного РОЗДІЛу того самого файлу?**
* а) Так, якщо вказати РОЗДІЛ except SyntaxError
* б) Ні, оскільки трансляція модуля повністю припиняється ще до старту виконання
* в) Так, але тільки у разі використання версії Python 3.10 і вище
* г) Так, якщо в блоці try викликати функцію eval()

**3. Яка низькорівнева C-структура в інтерпретаторі CPython представляє окремий стековий кадр під час виконання функцій та розкручування стека?**
* а) PyCodeObject
* б) PyTracebackObject
* в) PyFrameObject
* г) PyExceptionObject

**4. Який системний об'єкт формується віртуальною машиною під час розкручування стека викликів і зберігає ланцюжок фреймів із номерами рядків?**
* а) traceback
* б) sys.modules
* в) PyMethodDef
* г) co_varnames

**5. Яка системна функція модуля sys викликається інтерпретатором для виведення помилки у stderr, якщо виняток досяг дна стека без перехоплення?**
* а) sys.exit
* б) sys.excepthook
* в) sys.gettrace
* г) sys.set_coroutine_origin_tracking_depth

**6. Виявлення синтаксичної помилки SyntaxError у файлі призводить до того, що жодна інструкція байт-коду цього файлу взагалі не передається на виконання віртуальній машині PVM.**
* а) Правильно (True)
* б) Неправильно (False)

**7. Які з наведених класів помилок виникають на етапі компіляції вихідного коду в AST-дерево до старту виконання програми? (Оберіть усі правильні відповіді)**
* [ ] SyntaxError
* [ ] IndentationError
* [ ] ZeroDivisionError
* [ ] FileNotFoundError

**8. Вкажіть назву внутрішньої C-структури середовища CPython, каскадне знищення екземплярів якої відбувається під час процедури розкручування стека (stack unwinding).**
*(Впишіть відповідь)*: ______________________

**9. Встановіть відповідність між класом помилки та фазою її виникнення:**

| Клас помилки | Фаза виникнення |
| :--- | :--- |
| 1. SyntaxError | А. Фаза взаємодії з ядром ОС під час виконання системного виклику |
| 2. ZeroDivisionError | Б. Етап перевірки відступів лексичним аналізатором |
| 3. IndentationError | В. Етап синтаксичного розбору вихідного тексту компілятором |
| 4. FileNotFoundError | Г. Фаза динамічного виконання байт-коду віртуальною машиною |

**10. Що відбувається з локальними змінними стекового кадру PyFrameObject, якщо під час розкручування стека цей кадр знищується через відсутність обробника?**
* а) Змінні автоматично копіюються до простору імен глобального модуля
* б) Лічильники посилань локальних змінних декрементуються, ініціюючи деалокацію пам'яті
* в) Змінні залишаються заРОЗДІЛованими в пам'яті до перезавантаження ОС
* г) Пам'ять кадру записується у тимчасовий файл свопінгу на диску

---

### РОЗДІЛ 2. ІЄРАРХІЯ ВИНЯТКІВ, СПЕЦИФІЧНІСТЬ ТА ЛАНЦЮЖКИ

**11. Який клас є абсолютною вершиною ієрархії всіх об'єктів винятків у мові Python?**
* а) Exception
* б) BaseException
* в) StandardError
* г) SystemError

**12. Від якого класу повинні наслідуватися всі винятки прикладного рівня, обчислювальні помилки та власні класи користувача?**
* а) BaseException
* б) ArithmeticError
* в) Exception
* г) SystemExit

**13. Які з наведених винятків є прямими нащадками BaseException і не успадковуються від класу Exception? (Оберіть усі правильні відповіді)**
* [ ] SystemExit
* [ ] KeyboardInterrupt
* [ ] GeneratorExit
* [ ] ValueError
* [ ] RuntimeError

**14. Чому використання нетипізованого РОЗДІЛу `except:` або `except BaseException:` вважається грубим порушенням системного проектування?**
* а) Воно перехоплює сигнали завершення KeyboardInterrupt та SystemExit, перешкоджаючи плановій зупинці процесу
* б) Воно прискорює деградацію пам'яті віртуальної машини у 10 разів
* в) Воно забороняє використання конструкції finally
* г) Воно автоматично перезавантажує комп'ютер у разі збою

**15. Який базовий клас винятків об'єднує помилки трансляції індексів послідовностей (IndexError) та відсутності ключів асоціативних масивів (KeyError)?**
* а) LookupError
* б) ArithmeticError
* в) IndexError
* г) ValueError

**16. Спільним предком для ZeroDivisionError, OverflowError та FloatingPointError в ієрархії є клас:**
* а) ValueError
* б) ArithmeticError
* в) MathError
* г) SystemError

**17. Який клас стандартної ієрархії винятків виступає базовим для системних апаратних збоїв введення-виведення (FileNotFoundError, PermissionError, TimeoutError)?**
* а) IOErrorBase
* б) DeviceError
* в) OSError
* г) FileError

**18. За яким правилом слід розташовувати послідовні РОЗДІЛи except для коректної обробки споріднених класів винятків?**
* а) У порядку спадання специфічності (від конкретних підкласів до їхніх базових класів)
* б) У довільному порядку, оскільки інтерпретатор автоматично обирає найточніший збіг
* в) У порядку зростання специфічності (спочатку Exception, потім спеціалізовані класи)
* г) Виключно в алфавітному порядку назв класів

**19. Власні класи винятків для бізнес-логіки інженерних програм рекомендується успадковувати безпосередньо від BaseException.**
* а) Правильно (True)
* б) Неправильно (False)

**20. Яка синтаксична конструкція використовується для створення явного ланцюжка винятків зі збереженням первинної діагностики (PEP 3134)?**
* а) `raise CustomError() from original_err`
* б) `raise CustomError() with original_err`
* в) `raise CustomError() as original_err`
* г) `raise CustomError() in original_err`

**21. За допомогою якого виразу можна примусово відключити виведення первинного трасування контексту при збудженні нового винятку?**
* а) `raise CustomError() from None`
* б) `raise CustomError() without traceback`
* в) `raise CustomError() suppress`
* г) `raise CustomError() as None`

**22. Який атрибут об'єкта винятку зберігає посилання на попередній виняток при явному зв'язуванні конструкцією `raise ... from original_err`?**
*(Впишіть відповідь)*: ______________________

---

### РОЗДІЛ 3. РОЗДІЛИ TRY-EXCEPT-ELSE-FINALLY ТА КЕРУВАННЯ ПОТОКОМ

**23. За якої умови виконуються інструкції, розміщені у секції else конструкції try-except-else-finally?**
* а) Виключно тоді, коли в блоці try не виникло жодної виняткової ситуації
* б) Якщо в блоці try виник виняток, який не вдалося перехопити
* в) Щоразу після виконання РОЗДІЛу except
* г) Тільки якщо РОЗДІЛ finally завершився з помилкою

**24. За яких умов гарантовано виконується кодовий РОЗДІЛ finally?**
* а) Тільки якщо всі винятки були успішно перехоплені РОЗДІЛами except
* б) Безумовно завжди, незалежно від успіху РОЗДІЛу try, виникнення помилок чи операторів return
* в) Лише за умови відсутності оператора return у внутрішніх секціях
* г) Тільки якщо операційна система надіслала сигнал SIGINT

**25. Яке значення поверне функція, якщо в блоці try виконано `return 1`, у блоці except — `return 2`, а в блоці finally — `return 3`?**
* а) 1
* б) 2
* в) 3
* г) Виникне помилка переповнення стека

**26. Якщо всередині РОЗДІЛу try виник виняток, який успішно перехоплено секцією except, РОЗДІЛ finally все одно гарантовано виконається.**
* а) Правильно (True)
* б) Неправильно (False)

**27. Який синтаксис дозволяє перехопити кілька незалежних класів винятків в одному блоці except?**
* а) `except (ValueError, KeyError, TypeError):`
* б) `except [ValueError, KeyError, TypeError]:`
* в) `except ValueError or KeyError or TypeError:`
* г) `except ValueError | KeyError | TypeError:`

**28. Що виконує одиночний оператор raise без вказання класу чи аргументів всередині РОЗДІЛу except?**
* а) Збуджує виняток RuntimeError із порожнім повідомленням
* б) Повторно збуджує поточний активний виняток і продовжує розкручування стека
* в) Очищає трасування стека і завершує поточний фрейм
* г) Збуджує виняток BaseException

**29. Які секції можуть входити до повної конструкції захищеного РОЗДІЛу в Python? (Оберіть усі правильні відповіді)**
* [ ] try
* [ ] except
* [ ] else
* [ ] finally
* [ ] catch
* [ ] rescue

**30. Встановіть відповідність між секцією конструкції обробки помилок та її призначенням:**

| Секція | Призначення |
| :--- | :--- |
| 1. try | А. Секція операцій успіху, що виконується лише за відсутності винятків |
| 2. except | Б. РОЗДІЛ безумовної детермінованої фіналізації та вивільнення ресурсів |
| 3. else | В. Захищена область інструкцій, за якою здійснюється моніторинг винятків |
| 4. finally | Г. РОЗДІЛ фільтрації та локалізації конкретного типу виняткової ситуації |

**31. Якщо всередині РОЗДІЛу try спрацьовує інструкція return, РОЗДІЛ else все одно виконається перед виходом із функції.**
* а) Правильно (True)
* б) Неправильно (False)

**32. Якщо у блоці try виник виняток, для якого немає відповідного РОЗДІЛу except, коли саме виконається РОЗДІЛ finally?**
* а) РОЗДІЛ finally не виконається взагалі через аварійну зупинку
* б) РОЗДІЛ finally виконається перед тим, як виняток продовжить розкручувати стек викликів
* в) РОЗДІЛ finally виконається лише після звернення до sys.excepthook
* г) РОЗДІЛ finally виконається після перезапуску віртуальної машини

---

### РОЗДІЛ 4. ФАЙЛОВЕ ВВЕДЕННЯ-ВИВЕДЕННЯ, РЕЖИМИ ТА БУФЕРИЗАЦІЯ

**33. Що відбувається з існуючим файлом при його відкритті за допомогою функції `open(path, 'w')`?**
* а) Курсор встановлюється в кінець файлу для дозапису
* б) Файл РОЗДІЛується для читання іншими процесами без зміни розміру
* в) Вміст файлу миттєво обнуляється ядром ОС (прапорець O_TRUNC) ще до запису першого байта
* г) Збуджується виняток FileExistsError

**34. У чому полягає різниця між комбінованими режимами відкриття `'r+'` та `'w+'`?**
* а) `'r+'` відкриває файл для читання і запису без обнулення, а `'w+'` знищує початковий вміст файлу
* б) `'r+'` дозволяє працювати лише з бінарними даними, а `'w+'` — лише з текстом
* в) `'w+'` забороняє читання файлу після запису
* г) `'r+'` автоматично створює файл, якщо його не існує на диску

**35. Які три рівні абстракції формують архітектуру підсистеми потокового введення-виведення модуля io у CPython?**
* а) Socket I/O, Pipe I/O та Memory I/O
* б) Raw I/O (FileIO), Buffered I/O (BufferedReader/Writer) та Text I/O (TextIOWrapper)
* в) Block I/O, Character I/O та Network I/O
* г) Virtual I/O, Physical I/O та Kernel I/O

**36. Який типовий розмір внутрішнього буфера оперативної пам'яті встановлюється константою `io.DEFAULT_BUFFER_SIZE` у середовищі CPython?**
* а) 1024 байти (1 КБ)
* б) 8192 байти (8 КБ)
* в) 65536 байтів (64 КБ)
* г) 512 байтів

**37. Які події призводять до виштовхування (flush) накопичених байтів із програмного буфера Python у сторінковий кеш операційної системи? (Оберіть усі правильні відповіді)**
* [ ] Повне заповнення виділеного об'єму буфера пам'яті
* [ ] Явний виклик програмного методу `file.flush()`
* [ ] Закриття файлового дескриптора методом `file.close()` або вихід із РОЗДІЛу with
* [ ] Перехід процесу в інший потік виконання
* [ ] Виклик оператора ділення на нуль

**38. Яка кодова сторінка використовується за замовчуванням функцією `open(path, 'r')`, якщо параметр encoding не задано явно?**
* а) Завжди виключно строгий стандарт UTF-8
* б) Завжди однобайтне кодування ASCII
* в) Платформозалежна локаль ОС, повернута `locale.getpreferredencoding()`
* г) Кодування ISO-8859-1

**39. Виклик методу `file.write()` у мові Python гарантує негайний запис байтів безпосередньо у фізичні сектори накопичувача.**
* а) Правильно (True)
* б) Неправильно (False)

**40. Вкажіть дволітерний символьний рядок режиму функції open() для відкриття файлу на дозапис у кінець у бінарному форматі без обнулення вмісту.**
*(Впишіть відповідь)*: ______________________

**41. Який прапорець режиму відкриття файлу викликає виняток FileExistsError, якщо цільовий файл уже існує на накопичувачі?**
* а) `'x'`
* б) `'w'`
* в) `'a'`
* г) `'r'`

**42. Встановіть відповідність між символом режиму функції `open()` та семантикою доступу:**

| Символ режиму | Семантика доступу |
| :--- | :--- |
| 1. 'r' | А. Відкриття для дозапису; курсор встановлюється в кінець файлу |
| 2. 'w' | Б. Відкриття виключно для читання; файл повинен існувати |
| 3. 'a' | В. Ексклюзивне створення; збій у разі наявності файлу з таким ім'ям |
| 4. 'x' | Г. Відкриття для запису з обов'язковим обнуленням вмісту або створенням |

---

### РОЗДІЛ 5. МЕТОДИ SEEK, TELL ТА ПОЗИЦІОНУВАННЯ КУРСОРА

**43. Що повертає системний метод `file.tell()`?**
* а) Загальну кількість символів, які залишилося прочитати
* б) Поточну абсолютну позицію файлового курсору як цілочисельне зміщення в байтах
* в) Швидкість запису даних на накопичувач у байтах за секунду
* г) Системний номер дескриптора файлу в ядрі ОС

**44. Які базові точки відліку задають константи whence = 0, 1, 2 у виклику методу `file.seek(offset, whence)`?**
* а) 0 — початок файлу (SEEK_SET); 1 — поточна позиція (SEEK_CUR); 2 — кінець файлу (SEEK_END)
* б) 0 — кінець файлу; 1 — початок файлу; 2 — поточна позиція
* в) 0 — перший сектор диска; 1 — оперативна пам'ять; 2 — віртуальна пам'ять
* г) 0 — відносний зсув; 1 — абсолютний зсув; 2 — циклічний зсув

**45. Чому відносне переміщення курсору (whence=1 або whence=2) з ненульовим зміщенням заборонене у текстовому режимі читання UTF-8?**
* а) Операційна система не підтримує позиціонування у файлах розміром понад 2 ГБ
* б) Метод seek у текстовому режимі завжди викликає обнулення файлу
* в) Змінна довжина символів UTF-8 (від 1 до 4 байтів) унеможливлює лінійне зіставлення байтів і символів
* г) Кодування UTF-8 дозволяє читання лише в один бік (від кінця до початку)

**46. Який виняток генерує інтерпретатор при спробі виконати `f.seek(-10, os.SEEK_END)` у текстовому файлі (`io.TextIOWrapper`)?**
* а) IndexError
* б) ValueError
* в) io.UnsupportedOperation
* г) FileNotFoundError

**47. До якого наслідку призведе примусове позиціонування файлового курсору всередину мультибайтової послідовності UTF-8 з наступним викликом `read()`?**
* а) Інтерпретатор автоматично заповнить пропущений байт нулем
* б) Виникне аварійний виняток UnicodeDecodeError через розрив послідовності октет
* в) Курсор автоматично повернеться на початок файлу
* г) Символ буде прочитано як знак оклику '!'

**48. У бінарному режимі читання ('rb') переміщення курсору відносно кінця файлу з від'ємним зміщенням (наприклад, `seek(-128, os.SEEK_END)`) є повністю дозволеною операцією.**
* а) Правильно (True)
* б) Неправильно (False)

**49. Вкажіть назву методу файлового об'єкта, який повертає поточне зміщення курсору у вигляді непрозорого числа (cookie).**
*(Впишіть відповідь)*: ______________________

**50. За якою формулою розраховується зміщення байтів для прямого читання i-го бінарного запису фіксованого розміру R_size при розмірі заголовка файлу H_size?**
* а) H_size * i + R_size
* б) H_size + i * R_size
* в) (H_size + R_size) / i
* г) H_size + (i - 1) * R_size

---

### РОЗДІЛ 6. КОНТЕКСТНІ МЕНЕДЖЕРИ, RAII, ВИТОКИ ТА ЕКСПЛУАТАЦІЯ

**51. Який архітектурний патерн проектування реалізує синтаксична конструкція with у мові Python для детермінованого вивільнення ресурсів?**
* а) Singleton (Одинак)
* б) RAII (Resource Acquisition Is Initialization)
* в) Observer (Спостерігач)
* г) Factory Method (Фабричний метод)

**52. Які два магічні методи повинен реалізувати об'єкт для підтримки протоколу контекстного менеджера?**
* а) `__open__` та `__close__`
* б) `__enter__` та `__exit__`
* в) `__start__` та `__stop__`
* г) `__init__` та `__del__`

**53. Що повинен повернути метод `__exit__(self, exc_type, exc_val, exc_tb)` контекстного менеджера для повного придушення винятку, що виник у блоці with?**
* а) Логічне значення False
* б) Логічне значення True
* в) Об'єкт None
* г) Ціле число 0

**54. Якщо метод `__exit__` завершується без явного повернення значення (повертає None), виняток, що виник у блоці with, продовжує поширюватися стеком викликів.**
* а) Правильно (True)
* б) Неправильно (False)

**55. Які параметри передаються у метод `__exit__` у разі аварійного завершення тіла РОЗДІЛу with? (Оберіть усі правильні відповіді)**
* [ ] exc_type (тип класу збудженого винятку)
* [ ] exc_val (екземпляр об'єкта винятку з аргументами)
* [ ] exc_tb (об'єкт трасування стека traceback)
* [ ] sys_pid (ідентифікатор поточного процесу)
* [ ] file_fd (номер файлового дескриптора)

**56. Яка системна аварія виникає у процесі при неконтрольованому відкритті файлів без їх закриття за умови вичерпання ліміту дескрипторів RLIMIT_NOFILE?**
* а) MemoryError: Out of RAM
* б) RuntimeError: Stack overflow
* в) BufferError: Queue is full
* г) OSError: [Errno 24] Too many open files

**57. У чому полягає небезпека стану гонитви TOCTOU (Time-of-Check to Time-of-Use) при роботі з файлами?**
* а) Швидкість обертання жорсткого диска сповільнюється під час запису
* б) Стан файлу в ОС може змінитися іншим процесом між перевіркою (exists) та операцією (open)
* в) Таймер CPU втрачає синхронізацію з мережевим протоколом NTP
* г) Файловий дескриптор дублюється у віртуальній пам'яті

**58. Яке призначення системного виклику `os.fsync(file.fileno())`?**
* а) Очищення вмісту файлу до нульового розміру
* б) Примусове вимивання брудних сторінок із кешу ОС на фізичний енергонезалежний накопичувач
* в) Шифрування файлу відкритим ключем процесу
* г) Закриття доступу до файлу всім іншим користувачам ОС

**59. Яка послідовність операцій забезпечує транзакційну атомарність оновлення критичного файлу конфігурації у стандарті POSIX?**
* а) Відкриття у режимі 'w' -> write() -> close()
* б) Запис у тимчасовий файл у тому ж розділі -> flush() -> os.fsync() -> os.replace()
* в) Перевірка exists() -> відкриття у режимі 'r+' -> seek(0) -> write()
* г) Запис у тимчасовий файл -> видалення старого файлу -> пауза sleep(1) -> копіювання

**60. Встановіть відповідність між парадигмою інженерного проектування та її принципом:**

| Парадигма | Принцип |
| :--- | :--- |
| 1. EAFP | А. Вразливість часового розриву між верифікацією ресурсу та його використанням |
| 2. LBYL | Б. Оптимістичне виконання операції в блоці try з локалізацією у except |
| 3. RAII | В. Попередня перевірка умов через інструкції if перед виконанням дії |
| 4. TOCTOU | Г. Прив'язка звільнення ресурсів до завершення життєвого циклу об'єкта |

## Тема 9

### РОЗДІЛ 1. ОБ'ЄКТНА МОДЕЛЬ, PYOBJECT ТА МЕТАКЛАСИ

**1. Що означає твердження «все є об'єктом першого класу» (first-class object) в архітектурі CPython?**
* А) Будь-яка сутність (число, функція, клас) може динамічно створюватися, передаватися як аргумент і зберігатися у змінних
* Б) Усі класи повинні наслідуватися строго від одного спільного базового інтерфейсу
* В) Усі об'єкти компілюються у статичний машинний код до початку виконання програми
* Г) Об'єкти не можуть змінювати свій внутрішній стан після виділення пам'яті

**2. Які два обов'язкові системні поля містить заголовок базової C-структури PyObject у CPython?**
* А) ob_refcnt (лічильник посилань) та ob_type (покажчик на структуру типу)
* Б) ob_size (довжина) та ob_value (значення)
* В) ob_hash (хеш-код) та ob_dict (словник атрибутів)
* Г) ob_id (ідентифікатор) та ob_name (ім'я об'єкта)

**3. Яка сутність виступає метакласом (типом) для стандартних класів, оголошених через ключове слово `class`?**
* А) type
* Б) object
* В) classmethod
* Г) Meta

**4. Чи є правильним твердження: «У CPython метаклас type є екземпляром самого себе і одночасно успадковує базову поведінку від класу object»?**
* А) Правда (True)
* Б) Неправда (False)

**5. Які системні поля входять до структури PyVarObject для представлення об'єктів змінної довжини (рядків, списків, кортежів)?** *(Оберіть усі правильні відповіді)*
* А) ob_refcnt (лічильник посилань)
* Б) ob_type (покажчик на об'єкт типу)
* В) ob_size (кількість збережених елементів)
* Г) ob_dict (покажчик на глобальний простір)
* Д) ob_slots (масив статичних зміщень)

**6. Яку системну функцію виконує поле ob_refcnt у структурі PyObject?**
* А) Відстежує кількість активних зовнішніх посилань на об'єкт для автоматичного звільнення пам'яті
* Б) Зберігає розмір виділеної оперативної пам'яті у байтах
* В) Вказує на позицію об'єкта в ієрархії MRO
* Г) Забезпечує шифрування об'єкта у пам'яті процесу

**7. На яку системну структуру вказує покажчик ob_type у заголовку кожного об'єкта CPython?**
* А) PyTypeObject (структура, що визначає тип даних, поведінку та C-слоти операцій)
* Б) PyFrameObject (поточний стековий кадр виконання)
* В) PyCodeObject (незмінний байт-код функції)
* Г) PyDictObject (словник глобальних імен модуля)

**8. Вкажіть назву базової C-структури інтерпретатора CPython, від якої походять абсолютно всі об'єкти у динамічній пам'яті.**  
*(Дайте коротку відповідь)*

**9. Встановіть відповідність між елементом об'єктної моделі CPython та його роллю:**  
*Елементи:*
1. type  
2. object  
3. PyObject  
4. PyTypeObject  

*Ролі:*  
* А) Базовий клас на вершині ієрархії наслідування всіх типів  
* Б) Базова структура пам'яті для довільного екземпляра  
* В) Метаклас, який конструює та типізує самі класи  
* Г) Метаструктура опису семантики та C-слотів конкретного типу  

**10. Чи є правильним твердження: «У мові Python інструкція `class` виконується динамічно під час роботи програми, створюючи новий об'єкт-клас у купі»?**
* А) Правда (True)
* Б) Неправда (False)

---

### РОЗДІЛ 2. ЖИТТЄВИЙ ЦИКЛ ОБ'ЄКТА (__NEW__, __INIT__, __DEL__, SELF)

**11. У чому полягає принциповий розподіл відповідальності між методами `__new__` та `__init__`?**
* А) `__new__` виділяє фізичну пам'ять і повертає екземпляр, а `__init__` ініціалізує атрибути створеного об'єкта
* Б) `__init__` створює об'єкт у купі, а `__new__` реєструє його у збирачі сміття
* В) `__new__` виконується під час видалення об'єкта, а `__init__` — під час створення
* Г) `__new__` викликається тільки для числових типів, а `__init__` — для користувацьких класів

**12. Що повинен обов'язково повертати метод `__init__` у мові Python?**
* А) None (спроба повернути будь-яке інше значення збуджує виняток TypeError)
* Б) Посилання на створений екземпляр self
* В) Логічне значення True у разі успішної ініціалізації
* Г) Словник атрибутів екземпляра `__dict__`

**13. У яких інженерних сценаріях виникає необхідність явного перевизначення методу `__new__`?** *(Оберіть усі правильні відповіді)*
* А) Реалізація патерну проектування «Одинак» (Singleton)
* Б) Успадкування незмінних вбудованих типів даних (int, tuple, str)
* В) Керування пулом повторно використовуваних об'єктів для оптимізації пам'яті
* Г) Валідація типів вхідних аргументів через виняток ValueError
* Д) Очищення зовнішніх мережевих з'єднань перед виходом із програми

**14. Який перший обов'язковий аргумент приймає метод-алокатор `__new__`?**
* А) cls (посилання на клас, екземпляр якого створюється)
* Б) self (посилання на вже виділений об'єкт)
* В) args (кортеж позиційних параметрів)
* Г) mro (порядок вирішення методів)

**15. У який вираз інтерпретатор транслює виклик методу через екземпляр `inst.process(data)`?**
* А) `Class.process(inst, data)`
* Б) `Class.process(data)`
* В) `inst.process(Class, data)`
* Г) `process(inst, data)`

**16. Чи є правильним твердження: «Слово `self` є зарезервованим ключовим словом граматики Python і не може бути замінене іншим ідентифікатором»?**
* А) Правда (True)
* Б) Неправда (False)

**17. Який виняток виникає під час спроби викликати метод екземпляра `obj.method()`, якщо при оголошенні `def method():` не було вказано параметр `self`?**
* А) TypeError (повідомлення про невідповідність кількості позиційних аргументів)
* Б) NameError
* Г) AttributeError
* Д) SyntaxError

**18. Коли саме інтерпретатор CPython автоматично викликає спеціальний метод фіналізації `__del__(self)`?**
* А) Коли лічильник посилань на об'єкт ob_refcnt знижується до нуля
* Б) Одразу після виходу об'єкта за межі РОЗДІЛу try-except
* В) Кожні 60 секунд за системним таймером ОС
* Г) У момент виклику оператора `del` над змінною, навіть якщо існують інші посилання

---

### РОЗДІЛ 3. ТОПОЛОГІЯ АДРЕСНОГО ПРОСТОРУ ТА ЗАТІНЕННЯ АТРИБУТІВ

**19. До якого типу належить внутрішній словник простору імен класу `Class.__dict__`?**
* А) mappingproxy (незмінний словниковий проксі-об'єкт)
* Б) dict (стандартний змінюваний словник)
* В) tuple (незмінний кортеж пар)
* Г) set (множина ідентифікаторів)

**20. Яку функцію виконує словник `instance.__dict__` в об'єктній моделі Python за замовчуванням?**
* А) Зберігає унікальні атрибути конкретного екземпляра класу
* Б) Зберігає скомпільований байт-код методів класу
* В) Утримує посилання на метаклас type
* Г) Кешує результати роботи функції hash()

**21. У якому порядку здійснюється пошук атрибута під час виконання інструкції читання `obj.attr`?**
* А) `obj.__dict__` -> `Class.__dict__` -> базові предки за MRO
* Б) `Class.__dict__` -> `obj.__dict__` -> builtins
* В) MRO предків -> `Class.__dict__` -> `obj.__dict__`
* Г) Global namespace -> `obj.__dict__` -> `Class.__dict__`

**22. Що відбувається під час виконання операції запису `inst.attr = 10`, якщо `attr` спочатку було оголошено як атрибут класу?**
* А) У словнику `inst.__dict__` створюється новий локальний ключ `attr`, який затінює атрибут класу для цього екземпляра
* Б) Значення `attr` змінюється у словнику `Class.__dict__` для всіх екземплярів
* В) Збуджується виняток AttributeError через конфлікт імен
* Г) Атрибут стає приватним і перейменовується алгоритмом name mangling

**23. До яких наслідків призводить оголошення спільного списку як атрибута класу (`Class.shared_list = []`) при виклику `self.shared_list.append(data)` з різних екземплярів?**
* А) Усі екземпляри мутують один і той самий спільний об'єкт списку в пам'яті, змішуючи дані
* Б) Кожен екземпляр автоматично створює власну ізольовану копію списку
* В) Збуджується виняток PermissionError
* Г) Список автоматично заморожується у frozenset

**24. Чи є правильним твердження: «Виконання присвоєння `inst.attr = 100` завжди змінює значення однойменного атрибута в словнику `Class.__dict__`»?**
* А) Правда (True)
* Б) Неправда (False)

**25. Де саме інтерпретатор шукає атрибут, якщо він відсутній у словнику `instance.__dict__`?** *(Оберіть усі правильні відповіді)*
* А) У словнику простору імен поточного класу `Class.__dict__`
* Б) У словниках базових класів згідно з порядком MRO
* В) У глобальному просторі імен модуля
* Г) У локальному фреймі стека викликів

**26. Вкажіть назву спеціального системного атрибута екземпляра, який зберігає його внутрішню хеш-таблицю полів.**  
*(Дайте коротку відповідь)*

---

### РОЗДІЛ 4. ІНКАПСУЛЯЦІЯ, PEP 8 ТА NAME MANGLING

**27. Що означає префікс з одного символу підкреслення (`_attr`) в імені поля згідно з конвенцією PEP 8?**
* А) Атрибут призначений для внутрішнього використання класом та нащадками (непублічний API, захист за домовленістю)
* Б) Атрибут шифрується на рівні ядра ОС
* В) Інтерпретатор забороняє будь-який доступ до атрибута ззовні
* Г) Атрибут є константою, яку не можна переприсвоювати

**28. Який префікс ідентифікатора викликає активацію вбудованого механізму спотворення імен (name mangling)?**
* А) Два або більше провідних підкреслень за наявності не більше одного підкреслення в кінці (`__attr`)
* Б) Одне провідне підкреслення (`_attr`)
* В) Два підкреслення на початку і два в кінці (`__attr__`)
* Г) Префікс `private_` перед іменем поля

**29. Як компілятор Python перетворює приватне поле `__secret` у класі `Transceiver`?**
* А) `_Transceiver__secret`
* Б) `__secret_Transceiver`
* В) `_private_Transceiver_secret`
* Г) `Transceiver.__secret`

**30. Яка головна інженерна мета впровадження алгоритму спотворення імен (name mangling) у Python?**
* А) Запобігання колізіям імен атрибутів між базовими та похідними класами при наслідуванні
* Б) Криптографічний захист чутливих даних від читання зловмисником
* В) Прискорення доступу до пам'яті через зменшення довжини хеш-ключа
* Г) Автоматична серіалізація об'єктів у двійковий формат

**31. Чому системні методи на кшталт `__init__`, `__len__` та `__add__` виключені з дії алгоритму спотворення імен?**
* А) Щоб віртуальна машина PVM могла поліморфно викликати їх за фіксованими іменами системних C-слотів
* Б) Тому що вони займають надто багато місця в пам'яті
* В) Це обмеження версії Python 2, яке залишили для сумісності
* Г) Тому що dunder-методи можуть оголошуватися лише за межами класів

**32. Чи є правильним твердження: «Механізм name mangling повністю РОЗДІЛує читання приватного поля ззовні класу за будь-яких умов»?**
* А) Правда (True)
* Б) Неправда (False)

**33. Встановіть відповідність між стилем найменування атрибута та його архітектурним статусом:**  
*Стилі найменування:*
1. `public_attr`  
2. `_protected_attr`  
3. `__private_attr`  
4. `__special_attr__`  

*Статуси:*  
* А) Захищений атрибут за конвенцією PEP 8, внутрішня деталь реалізації  
* Б) Публічний інтерфейс, відкритий для читання та модифікації  
* В) Системний магічний dunder-атрибут протоколів мови Python  
* Г) Приватний атрибут, що підлягає алгоритму спотворення імен (name mangling)  

**34. Який виняток збуджується під час спроби прямого звернення `inst.__data` ззовні класу, де оголошено `self.__data`?**
* А) AttributeError
* Б) PermissionError
* В) KeyError
* Г) SecurityError

---

### РОЗДІЛ 5. НАСЛІДУВАННЯ, РОМБ, SUPER() ТА ЛІНЕАРИЗАЦІЯ C3 MRO

**35. У чому полягає «проблема ромба» (diamond problem) при множинному наслідуванні?**
* А) У неоднозначності порядку виклику методів та дублюванні ініціалізації спільного базового предка
* Б) У неможливості створення більше двох нащадків від одного класу
* В) У РОЗДІЛуванні роботи збирача сміття через циклічні посилання
* Г) У забороні використання абстрактних методів у проміжних класах

**36. Яку функцію виконує вбудований проксі-об'єкт `super()` у мові Python?**
* А) Делегує виклик методу наступному класу згідно з порядком MRO викликаючого екземпляра
* Б) Завжди викликає метод строго лівого батьківського класу
* В) Перетворює довільний метод на класовий метод `@classmethod`
* Г) Створює ізольовану копію базового класу в пам'яті

**37. Який алгоритм використовується інтерпретатором CPython для розрахунку порядку вирішення методів (MRO)?**
* А) Алгоритм лінеаризації C3 (C3 Linearization)
* Б) Пошук у глибину (Depth-First Search)
* В) Пошук у ширину (Breadth-First Search)
* Г) Алгоритм Дейкстри

**38. Які обов'язкові властивості забезпечує алгоритм лінеаризації C3?** *(Оберіть усі правильні відповіді)*
* А) Монотонність (порядок предків зберігається для всіх підкласів)
* Б) Збереження локального порядку слідування батьківських класів
* В) Випадковий вибір гілки у разі виникнення колізій
* Г) Обов'язковий дублюючий виклик спільних базових класів

**39. Який виняток генерує Python під час спроби оголосити клас із суперечливою ієрархією наслідування, яку неможливо лінеаризувати?**
* А) TypeError (повідомлення: Cannot create a consistent method resolution order)
* Б) SyntaxError
* В) RecursionError
* Г) NameError

**40. Нехай оголошено класи `A`, `B(A)`, `C(A)`, `D(B, C)`. Яким буде порядок лінеаризації L(D)?**
* А) `[D, B, C, A, object]`
* Б) `[D, B, A, C, object]`
* В) `[D, C, B, A, object]`
* Г) `[D, B, A, object]`

**41. До чого призводить жорсткий виклик `BaseClass.__init__(self)` замість `super().__init__()` у кооперативному множинному наслідуванні?**
* А) Розриває ланцюжок лінеаризації MRO, що може призвести до пропуску ініціалізації сусідніх гілок або повторних викликів
* Б) Викликає помилку компіляції байт-коду SyntaxError
* В) Автоматично РОЗДІЛує доступ до атрибутів екземпляра
* Г) Знищує об'єкт у пам'яті до завершення роботи функції

**42. Який спеціальний атрибут класу повертає кортеж лінеаризованого порядку дозволу методів?**
* А) `__mro__`
* Б) `__order__`
* В) `__bases__`
* Г) `__hierarchy__`

**43. Вкажіть назву методу класу, виклик якого повертає лінеаризований список предків MRO у вигляді списку.**  
*(Дайте коротку відповідь)*

**44. Чи є правильним твердження: «У разі множинного наслідування виклик `super()` усередині методу одного класу може передати керування методу сусіднього класу (sibling), який не є його прямим предком»?**
* А) Правда (True)
* Б) Неправда (False)

---

### РОЗДІЛ 6. КАЧИНА ТИПІЗАЦІЯ ТА DUNDER-МЕТОДИ

**45. Що є головним критерієм сумісності об'єкта з протоколом в концепції качиної типізації (Duck Typing)?**
* А) Наявність у об'єкта необхідних методів та атрибутів у момент виклику, незалежно від його наслідування
* Б) Обов'язкова наявність спільного абстрактного предка
* В) Збіг фізичного розміру структури PyObject у пам'яті
* Г) Явна попередня реєстрація типу у словнику `sys.modules`

**46. Яка пара dunder-методів реалізує протокол ітерування в Python?**
* А) `__iter__` та `__next__`
* Б) `__enter__` та `__exit__`
* В) `__getitem__` та `__len__`
* Г) `__init__` та `__call__`

**47. Які методи обов'язково повинен реалізувати клас для підтримки роботи з інструкцією `with`?**
* А) `__enter__` та `__exit__`
* Б) `__open__` та `__close__`
* В) `__start__` та `__stop__`
* Г) `__acquire__` та `__release__`

**48. Що повинен повернути метод перевантаження оператора `__add__(self, other)`, якщо тип операнда `other` не підтримується?**
* А) NotImplemented (системну константу, яка сигналізує PVM спробувати викликати `__radd__`)
* Б) Згенерувати виняток `raise TypeError`
* В) Повернути логічне значення False
* Г) Повернути об'єкт None

**49. Встановіть відповідність між dunder-методом та операцією, яку він перевантажує:**  
*Методи:*
1. `__abs__`  
2. `__getitem__`  
3. `__eq__`  
4. `__repr__`  

*Операції:*  
* А) Доступ за індексом або ключем `obj[key]`  
* Б) Обчислення модуля або евклідової норми `abs(obj)`  
* В) Офіційне строкове представлення об'єкта для розробника  
* Г) Операція перевірки рівності `obj == other`  

**50. Чи є правильним твердження: «Якщо лівий операнд у виразі `a + b` повертає константу NotImplemented, інтерпретатор автоматично намагається викликати реверсивний метод `b.__radd__(a)`»?**
* А) Правда (True)
* Б) Неправда (False)

---

### РОЗДІЛ 7. ДЕСКРИПТОРИ, PROPERTY, CLASSMETHOD ТА STATICMETHOD

**51. У чому полягає відмінність між дескриптором даних (data descriptor) та дескриптором без даних (non-data descriptor)?**
* А) Дескриптор даних визначає метод `__set__` або `__delete__`, а дескриптор без даних — тільки `__get__`
* Б) Дескриптор даних зберігає числа, а дескриптор без даних — рядки
* В) Дескриптори даних можуть оголошуватися лише всередині функцій
* Г) Дескриптори без даних не мають доступу до аргументу `instance`

**52. Який пріоритет має пошук атрибута, якщо в класі визначено дескриптор даних із методом `__set__`, а в `instance.__dict__` вже існує однойменний ключ?**
* А) Дескриптор даних має вищий пріоритет і викликається раніше за словник екземпляра
* Б) Словник екземпляра повністю затінює дескриптор даних
* В) Збуджується виняток AttributeError через колізію
* Г) Викликається випадковий метод залежно від стану пам'яті

**53. Що створює вбудований декоратор `@property` в тілі класу?**
* А) Дескриптор даних, що дозволяє звертатися до методів валідації через синтаксис звичайного атрибута
* Б) Статичну копію функції в модулі
* В) Приватне поле, захищене алгоритмом name mangling
* Г) Окремий процес операційної системи для фонових обчислень

**54. Який перший аргумент автоматично отримує метод, декорований `@classmethod`, під час виклику?**
* А) cls (посилання на сам об'єкт-клас)
* Б) self (посилання на конкретний екземпляр)
* В) mro (порядок лінеаризації)
* Г) None

**55. Як поводиться метод, оформлений за допомогою декоратора `@staticmethod`?**
* А) Працює як звичайна функція в просторі імен класу, не отримуючи автоматично ні self, ні cls
* Б) Отримує першим аргументом глобальний словник модуля
* В) Автоматично РОЗДІЛує потік виконання за допомогою GIL
* Г) Викликається виключно один раз під час завантаження програми

**56. Яка основна інженерна сфера застосування декоратора `@classmethod`?**
* А) Створення альтернативних конструкторів (фабричних методів) для різних форматів даних
* Б) Обчислення контрольних сум бінарних РОЗДІЛів
* В) Заборона створення екземплярів класу
* Г) Створення глобальних змінних

---

### РОЗДІЛ 8. КРИТИЧНІ БАР'ЄРИ: __SLOTS__, ВБУДОВАНІ С-ТИПИ ТА GIL

**57. Яким чином декларація атрибута `__slots__` оптимізує використання пам'яті екземплярами класу?**
* А) Усуває створення словника `__dict__`, замінюючи його статичним масивом покажчиків фіксованого розміру
* Б) Автоматично переводить усі змінні у формат float32
* В) Стискає екземпляр класу за допомогою алгоритму zip
* Г) Вивантажує невикористовувані поля у файл підкачки

**58. Що станеться, якщо клас-нащадок успадковує клас із `__slots__`, але сам не оголошує власний атрибут `__slots__`?**
* А) Екземпляри нащадка отримають стандартний словник `__dict__`, що нівелює оптимізацію пам'яті для нових полів
* Б) Інтерпретатор збудить виняток TypeError під час компіляції класу
* В) Нащадок повністю втратить доступ до полів батьківського класу
* Г) Нащадок автоматично успадкує фіксовану пам'ять без створення `__dict__`

**59. Чому пряме успадкування від вбудованого класу `dict` і перевизначення `__setitem__` не перехоплює виклик методу `dict.update()`?**
* А) Методи вбудованих С-типів у CPython реалізовані на C і викликають низькорівневі C-функції в обхід методів Python-рівня
* Б) Тому що метод `update()` заборонено викликати в класах-нащадках
* В) Через дію алгоритму name mangling
* Г) Тому що словники не підтримують поліморфізм

**60. Чи є правильним твердження: «Завдяки наявності Global Interpreter Lock (GIL) у CPython операція інкременту атрибута `self.counter += 1` є абсолютно атомарною і потокобезпечною»?**
* А) Правда (True)
* Б) Неправда (False)
